[package]
name = "rayon-fanout"
edition = "2024"
version = "0.1.0"
publish = false

[dependencies]
tokio = { version = "1.51.0", default-features = false, features = ["time", "rt"] }
rayon = "1"
dial9 = { path = "../../dial9", default-features = false, features = [
    "tokio",
    "cpu-profiling",
] }
