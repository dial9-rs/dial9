# Cold-start verifier findings (raw, 2026-07-08)

## T01 impl=partial dod=no
- [MISSING] How to enable `aggregation_enabled` on the dev-server. The DoD requires each new row's access path be verified against the running dev-server, and #570 Tokio Stats is explicitly CONDITIONAL on aggregation_enabled; the given launch command (`PORT=3001 cargo run -p dial9-viewer --bin dev-server --features dev-server`) does not say how to turn aggregation on, so the Tokio Stats access path may not be live-verifiable as instructed.
- [MISSING] Whether the drift set is the 6 named commits or all 9 returned by the provided git command. #607 (cross-region S3), #597 (assume-roles), and #586 (browse overflow/path-traversal) land in the `--since=2026-06-26` window but are omitted from the 'notably' list; the agent needs to know if these three require UI-visibility triage/rows too.
- [REFS] Ticket says the inventories were 'snapshotted ~2026-06-26', but `features/01-index-html.md` records a live validation on 2026-06-30 and asserts 'no documented feature was missing' — yet #570 (06-27), #582 (06-27), and #585 (06-29) landed before that validation and are still absent from the inventory. The '~2026-06-26' snapshot date is inaccurate for file 01 (though the --since window still captures the commits).
- [REFS] Ticket labels '#600 (multi-trace load pipelining)', but the commit (c0a97e1) is titled '...(#595) (#600)'; the substantive change is PR #595, with #600 as the merge/follow-up number.
- [AMBIG] 'for each drift commit ... add/update inventory rows' vs 'Goal: 9 UI-touching commits' vs a 6-item 'notably' list: unclear whether the agent must process exactly the 6 enumerated commits or all 9 the git command returns. This changes scope and which files get touched.
- [AMBIG] 'add/update inventory rows' implies some rows are edits to existing rows, but the DoD only says 'each new row's access path verified against the running dev-server'. Whether updated (non-new) rows also require live re-verification is unclear.
- [DODISS] 'every UI-visible change from the drift commits has an inventory row' is not mechanically checkable as written: 'UI-visible' requires human judgment, and the drift-commit set itself is ambiguous (6 named vs 9 in the log), so completeness cannot be objectively verified.
- [DODISS] 'each new row's access path verified against the running dev-server' is not executable for the CONDITIONAL Tokio Stats rows (#570) unless aggregation_enabled can be turned on, which the ticket does not explain how to do.

## T02 impl=partial dod=no
- [MISSING] Which Vite mechanism/plugin implements the 'static-copy list' (e.g. vite-plugin-static-copy vs public/ verbatim copy). This is a new npm dependency that per constraint S1 needs per-dep justification, but the ticket names no chosen approach.
- [MISSING] The source-location contract for demo-trace.bin and flamegraph.css: they live at ui/ root today, but architecture 2.1 places them under public/. The ticket does not say whether to move them into public/ or copy them from root, which changes the config.
- [REFS] 'rust-embed #[folder] -> ui/dist/ (src/server/mod.rs:44)': the #[folder = "ui/"] attribute is actually at src/server/mod.rs:49; line 44 is inside region_from_head_bucket(). (The ADR and 01-technical-constraints H1 repeat the same stale :44 anchor.)
- [REFS] serve.py deletion is T04's, not T02 — but note dial9-viewer/serve.py does not exist in the tree today, so any assumption it is still present is inaccurate (does not affect T02 directly).
- [AMBIG] demo-trace.bin / flamegraph.css handling: the ticket lists them in the Vite 'static-copy list into dist/', while architecture 2.1 handles them via public/ (verbatim copy). These are two different Vite config mechanisms producing the same result; which one to use is unstated.
- [AMBIG] DoD 'parity: T12 census old-vs-new shows zero diff' — T02 migrates no page (all three pages stay legacy, merely copied into dist/), so what constitutes 'new' vs 'old' for the census is unclear (presumably legacy-served-from-ui/ vs legacy-served-from-ui/dist/).
- [DODISS] 'parity: T12 census old-vs-new shows zero diff' is not mechanically checkable at T02 completion: the T12 parity tooling does not exist yet, and T12 itself depends on T02 (and T01). The ticket declares 'Deps: none' yet the DoD requires T12 tooling — a circular/forward dependency that blocks executing this DoD item.
- [DODISS] 'serves all three pages exactly as today' relies on the same absent T12 census for an objective 'exactly'; without it the check is subjective/manual.
- [DODISS] 'binary no longer contains test_*.js — verifiable via strings or embed listing' is checkable but only after npm run build populates dist/; fine as long as the build step is run, but it presupposes a successful build not otherwise gated in the DoD.

## T03 impl=yes dod=yes
- [MISSING] Which release workflow actually publishes: release.yml delegates publishing to release-plz (a single 'Run release-plz' step, no explicit `cargo publish`), and binary builds live in a separate matrix job. The ticket does not say where in the release-plz flow the `npm ci && npm run build` step must sit to guarantee dist exists before publish, so the exact injection point must be inferred.
- [MISSING] Whether the `tsc --noEmit`, `vitest run`, and `npm run build` checks should be three separate CI jobs or one combined 'ui' job, and the exact job name(s) to add to the `ci-pass` needs list (line 318).
- [REFS] Ticket cites the `ci-pass` gate at '~:316'; it is actually at line 315 (needs list at 318). Trivial and hedged with '~', but not exact.
- [REFS] Context lists `release-pr.yml` as a workflow to touch, but that workflow only runs `release-plz release-pr` to open the release PR — it neither builds binaries nor publishes, so there is nothing there for a UI build stage to precede. Its inclusion is misleading.
- [AMBIG] 'wire into the ci-pass needs list' plus 'CI jobs: tsc --noEmit, vitest run, npm run build' leaves open whether these are 3 jobs or 1, changing both workflow structure and the ci-pass needs entries.
- [AMBIG] 'npm ci && npm run build BEFORE cargo publish and before binary builds' — release.yml has no literal `cargo publish` step (it is inside release-plz) and binary builds are a distinct matrix job; the placement/duplication of the build step across these two paths is a genuine implementation fork.
- [AMBIG] 'vitest run (may be a no-op until T10)' — unclear whether to add a real vitest invocation now (which would fail without T10's config) or a placeholder; T10 itself depends on T03, so the intended sequencing of the vitest CI line is ambiguous.
- [DODISS] 'a dry-run release build (workflow_dispatch or local simulation of the steps) produces a crate package containing ui/dist' — 'local simulation of the steps' is not a defined, mechanically-run command; since publishing is via release-plz, there is no first-class dry-run, so the checker must improvise (e.g. `cargo package -p dial9-viewer` + inspect archive). Verifiable in practice but not mechanically specified.

## T04 impl=yes dod=no
- [MISSING] The dev-server binary is invoked with `--features dev-server` in the inventory docs (features/01-index-html.md:35); the ticket doesn't state whether the `npm run dev`/`dev:embedded` scripts must launch it and with which feature flags, so the exact command the proxy targets is left to inference.
- [MISSING] No `src/` module exists in chunk 1 (page conversion is explicitly out of scope and T02 only creates skeleton dirs), so the ticket gives no concrete module to edit to demonstrate the HMR/proxy loop required by the DoD.
- [REFS] `dial9-viewer/src/lib.rs:102` is cited for `with_dev_ui_dir`, but line 102 is unrelated `AggContext` construction code; the actual `with_dev_ui_dir` call in lib.rs is at line 244 and the method is defined at src/server/mod.rs:128.
- [REFS] `src/bin/dev_server.rs:102` is off by one: line 102 is `force_path_style: true,`; the `with_dev_ui_dir(ui_dir)` call is at line 103.
- [REFS] The ticket asserts `serve.py` 'is not part of any documented workflow', but it is documented in docs/ui-inventory/features/01-index-html.md (lines 35 and 284) as the static-only local-serving alternative to the dev-server; chunk-3-post.md:192 also tracks removing stale serve.py references.
- [AMBIG] '`with_dev_ui_dir` repointed at `ui/dist`': dev_server.rs currently hardcodes the UI dir to `.join("ui")` (line 102). It is unclear whether dev:embedded should change that default outright, or add an env var/flag to select `ui/dist`, which affects whether the plain (non-embedded) dev-server behavior changes.
- [AMBIG] 'edit a legacy page file -> refresh shows it (embedded mode)': in embedded mode assets are served from `ui/dist`, which only receives legacy pages via Vite's static-copy on `vite build --watch`; whether an editor's raw legacy file edit is expected to appear directly or only after the watch rebuild is not spelled out.
- [DODISS] All three DoD items are phrased as manual 'demonstrated' observations (edit->refresh, edit->HMR, /api/* works) with no script, test, or automated assertion — they are not mechanically checkable as written.
- [DODISS] 'edit a `src/` module -> HMR applies it' cannot be executed as stated because no `src/` module exists in chunk 1 (page code is out of scope); the reviewer must fabricate a throwaway module, which the DoD does not define.

## T05 impl=yes dod=no
- [MISSING] Source-of-truth for the 7 core modules with NO skill doc (format.js, heatmap.js, prefix_detect.js, creds.js, panel_layout.js, flamegraph.js, flamegraph_export.js): the ticket names the trace-loading/trace-analysis skills as the shape source, but those only cover ParsedTrace + trace_analysis. The public-API shapes for the other 7 must be reverse-engineered from the code + HTML call sites, which the ticket does not anchor to file:line.
- [AMBIG] Boundary vs T06 is fuzzy: T05 declares 'what the core returns' (so the ParsedTrace interface must live in trace_parser/trace_analysis .d.ts) while T06 owns types/trace.d.ts and 'refines app-level shapes'. Whether the canonical ParsedTrace interface definition belongs in T05's src/types core declarations or in T06's types/trace.d.ts (with T05 merely referencing it) is not pinned down and changes what T05 actually writes.
- [AMBIG] Architecture 2.6 names the parsed-shape file 'types/trace.d.ts', but T05's Work says core declarations go in 'src/types/'. Whether these are the same directory (src/types/trace.d.ts) or two separate locations is not stated.
- [DODISS] 'declarations reviewed against at least one real call site per module' is a manual review judgement, not mechanically checkable — there is no automated gate asserting each declared API is exercised by a call site (the tsc probe only proves the API imports resolve, not that they match a real call site).
- [DODISS] The Work rule 'never any without an inline justification comment' has no corresponding mechanical DoD gate; tsc --noEmit passes even with unjustified `any`, so this constraint is not enforced by any listed check.

## T06 impl=yes dod=no
- [AMBIG] 'kind-discriminated unions for events/panels' does not name the discriminant field or give a single enumerated list; the event kinds must be assembled from 2.6 (workers, polls, spans, custom events, CPU/heap samples, sched events, queue series, block-in-place gaps) and the panel kinds from 2.3 (spans, custom events, CPU, queue, task detail), so two implementers could produce different union shapes.
- [AMBIG] Path is written as 'types/trace.d.ts' / 'types/state.d.ts' but the actual directory per architecture 2.1 and T05 is 'src/types/'; harmless but two readings exist.
- [AMBIG] 'layout geometry types wrapping panel_layout outputs' leaves open whether these live in types/state.d.ts, types/trace.d.ts, or a separate file, and how much of panel_layout's output to model.
- [DODISS] 'exhaustive-switch probe compiles (adding a variant breaks the build - demonstrated in a test)' is not mechanically checkable as stated: a committed test that adds a union variant would break the build and therefore cannot be a passing test. It needs an explicit negative-compilation mechanism (e.g. @ts-expect-error or tsd type tests), which the DoD does not specify, so 'demonstrated in a test' has no unambiguous pass/fail procedure.
- [DODISS] 'tsc --noEmit green' is checkable but depends on the T02 scaffold and T05 declarations existing; on their own the T06 files cannot be type-checked, so the check is only executable once those deps are in place (acceptable given deps, but not self-contained).

## T07 impl=yes dod=no
- [MISSING] The N18 dev-build assertion hook must 'flag renders triggered outside the scheduler', but the store has no reference to any renderer and page-wiring is explicitly out of scope. The mechanism by which the store detects a 'render' (vs. a plain read/update) is not specified anywhere in 2.2, N18, or F2, so the exact detection contract to implement/test is undefined.
- [MISSING] The `derived(deps, compute)` helper's invalidation granularity is unspecified: F5/2.2 say 'keyed by the slices it depends on' but neither states whether invalidation is per-slice or per-field-within-slice (viewport-window vs whole viewport slice), which changes the cache key design.
- [AMBIG] 'transient channel bypassing full notification' (DoD) vs 2.2's 'crosshair RAF channel': it is unclear whether transient updates must run on a SEPARATE RAF channel from the main coalesced tick, or simply skip subscriber notification entirely. The two readings produce different scheduler implementations.
- [AMBIG] 'a derived(deps, compute) cache helper invalidated by slice change' — 'slice change' could mean identity change of the slice object or an actual value diff; with 'trace replaced wholesale, never mutated' the former is implied but not stated, affecting whether the helper needs value comparison.
- [DODISS] 'a dev-build assertion hook (N18) that flags renders triggered outside the scheduler' appears in Work but has no corresponding DoD test item; combined with the missing detection contract, there is no mechanically-checkable acceptance criterion for the N18 hook (the '100% of store branches covered' item only guarantees code execution, not that it correctly flags an out-of-scheduler render).

## T08 impl=yes dod=yes
- [MISSING] The build/module scaffolding that defines where `src/lib/canvas/` lives and how Vitest is wired (T02 output) is only a transitive dependency; T08 lists Deps as T05, T06 but not T02, so the JS/TS project root is assumed rather than established by a listed dep.
- [REFS] Context says the palette utilities are re-exported from the frozen core alongside downsampling/coalescer, but the poll heatmap palette is `pollHeatmapColorQuantized`/`pollHeatmapColor` in trace_analysis.js, not in heatmap.js; ticket's grouping is loose but the functions do exist and are exported, so not fatally wrong.
- [AMBIG] 'palette utilities'/'palettes' (plural) is unscoped: it is unclear whether this means only the poll-heatmap palette (trace_analysis.js) or also the density palette (heatmap.js `densityColor`) and flamegraph hue palette.
- [AMBIG] DoD 'stroke batcher emits <= width vertices' — 'width' is ambiguous between full canvas pixel width and drawW (LABEL_W-adjusted plot width); the bound differs by the gutter/scrollbar.

## T09 impl=yes dod=no
- [MISSING] Multi-host title.ts test case: Live-validation I3 records the multi-host `host=` drop branch as never exercised (demo has one host) and there is no multi-host fixture in the repo; the implementer must synthesize inputs from the ported inline logic with no captured expected output to check against.
- [MISSING] Consumer contract for the new `layout:'unknown'` discriminant: keys.ts must emit it, but the ticket does not say what fields (service/host/bootId) an `unknown`-layout result should carry, only that it must not be 'shifted'. Whether they should be empty, raw, or the whole key is unspecified (T15 owns display, but keys.ts shape is decided here).
- [REFS] features/01 I2 cites parseKey at `index.html:794-847`, but parseKey is actually at line 1006 (794-847 is heatmap interaction setup). T09 depends on T05/T06 but NOT T01 (which refreshes inventory line anchors), so a cold-start implementer following the cited anchor lands on the wrong code. Row content is still accurate.
- [REFS] features/01 I3 cites traceTitleParams at `index.html:1457-1473`; actual location is line 1686.
- [REFS] features/01 I4 cites objectTraceUrls at `index.html:1482-1489`; actual location is line 1713.
- [AMBIG] 'load.ts fetch+gunzip+concat against the existing test_fetch_traces.js fixtures pattern': test_fetch_traces.js uses an in-test fetch() mock mapping URL->bytes, not on-disk fixture files. 'fixtures pattern' could mean reuse the mock approach or produce new fixture files — different test scaffolding.
- [DODISS] The Work lists the full 2.7 seam (load/reparse/query/analysis/keys/title + barrel), but the DoD only verifies keys.ts, title.ts, and load.ts. There is no mechanically checkable DoD item confirming reparse.ts, query.ts, and analysis.ts exist or behave correctly — those deliverables ship unverified.
- [DODISS] The Work mandates an ESLint (or grep-based CI) rule forbidding `src/` imports of core files outside lib/trace + lib/canvas re-exports, but no DoD item exercises or asserts that this guardrail actually fires. The rule's correctness is not mechanically checked.

## T10 impl=yes dod=no
- [MISSING] vitest config target: T10 says 'vitest config in vite.config.ts (node env default)' but does not specify how the CJS-guard frozen core (browser-global scripts with module.exports guards, per H2) is imported under vitest's node env — whether default CJS interop suffices or a test alias/config is needed. Not shown in the referenced context.
- [MISSING] test_parse_key.js currently loads parseKey by reading index.html through Node's `vm` module (not by importing a core file). The ticket gives no guidance on how to preserve that extraction pattern under vitest, which matters since parseKey is not yet a standalone importable module (T09 ports it later).
- [REFS] T10 instructs 'delete migrated originals + their e2e-trace-tests.sh lines in the same PR' and cites scripts/e2e-trace-tests.sh as 'the current registration list,' but 6 of the 8 selected files (test_format.js, test_parse_key.js, test_heatmap.js, test_panel_layout.js, test_poll_color.js, test_time_range.js) are NOT registered in that script; only test_creds.js and test_prefix_detection.js appear there. The other 6 have no script lines to delete and are not currently run in CI at all.
- [REFS] Context (via ADR section 7 / N13) refers to '~24 plain-node test_*.js suites'; the repo actually contains 29 test_*.js files. Minor count drift that affects the 'first ~8 of N' framing.
- [AMBIG] 'keep un-migrated files running via the script (dual-runner CI)' plus vitest auto-discovery is ambiguous about whether the newly migrated *.test.ts/js are expected to also be excluded from / coexist with the node-script remainder without double-running any shared behavior — the boundary between the two runners for this batch is not spelled out.
- [AMBIG] File extension choice ('*.test.ts/*.test.js') is left open, so two implementers could produce different (TS vs JS) test forms for the same suite.
- [DODISS] 'review diff test-by-test: every assertion preserved or consciously strengthened, none dropped' is a subjective manual-review criterion, not mechanically checkable.
- [DODISS] 'CI runs both' is not verifiable within T10 alone: the CI wiring for vitest lives in T03, so whether CI actually runs both runners cannot be confirmed by anything produced in this ticket without inspecting T03's workflow changes.

## T11 impl=partial dod=no
- [MISSING] The concrete Vitest config + migration pattern established by T10 (colocation, CJS interop wiring, describe/expect conversion of test_harness assertions) — T11 says 'same as T10' but that landed setup is the actual template and is not reproduced here; only T10's title is available at cold start.
- [MISSING] Which of the ~29 dial9-viewer/ui/test_*.js files are real suites to migrate versus support files (e.g. test_harness.js is the assertion helper, not a suite). The work list is explicitly non-exhaustive ('...'), so the full target set must be derived by glob, and the treatment of the helper is unspecified.
- [REFS] Ticket claims 'stress-test.yml runs test_trace_integrity.js <file> and test_task_lifecycle.js ... switch the file argument from argv to an env var.' Only test_trace_integrity.js reads a file from argv (process.argv[2] at line 52). test_task_lifecycle.js has NO argv/file-argument usage at all — it is invoked with no argument in stress-test.yml. There is no argv file argument on test_task_lifecycle.js to switch.
- [REFS] The actual stress-test.yml loop invokes 'node .../test_trace_integrity.js' with NO file argument (it defaults to demo-trace.bin). The 'test_trace_integrity.js <file>' form appears only inside a debugging comment and the generated README.txt reproduction text, not in a runtime workflow invocation — so the premise that the workflow passes a file argument at runtime is inaccurate.
- [AMBIG] 'switch the file argument from argv to an env var ... and update the workflow': since the workflow never passes a file argument at runtime (only default demo-trace.bin, plus a repro comment), it is unclear whether the change is limited to the test's argv-reading code, the repro comment/README text, the workflow step, or all three.
- [AMBIG] DoD 'zero test_*.js files remain (or an explicit documented exception list)': unclear whether the test_harness.js helper (a support module matching the glob, described in T10 as 'being replaced') must be deleted, migrated, or listed as an exception.
- [DODISS] 'assertion-preservation review as in T10' is a subjective manual diff review (every assertion preserved or consciously strengthened, none dropped), not a mechanically checkable pass/fail gate.

## T12 impl=partial dod=no
- [MISSING] The audit-phase 'prototypes' T12 says to productize (journey driver, affordance census dumper, axe scan) are not in the repo - they are only referenced as session-scratchpad artifacts (ux/*.png, ux_results.json, verify_results.json) in 04-ux-findings.md Method; no reusable script exists, so all four layers must be built from scratch with no reference implementation.
- [MISSING] For layer (c) behavioral differ: which of the 8 named journeys (J1-J8) to encode as the driving script, and the exact field-level data readouts to diff per journey (the ticket lists 'event/worker/span/POI counts, task-detail numbers' but no per-page enumeration of where those values live).
- [MISSING] The row-walker needs a machine-drivable access path per inventory row, but features/01 rows express access paths only in prose ('Controls bar -> Prefix field'); there is no selector/action mapping to drive them mechanically.
- [REFS] T12 Context claims 'the plan's four layers (chunk-1 header of this file + ADR section 7)'; neither the chunk-1 header (lines 1-26) nor ADR-0004 section 7 enumerates or defines four verification layers - the four layers (a-d) are defined only inside T12's own Work section.
- [REFS] T12 Context says 'prototypes exist from the audit phase ... session-scratchpad scripts referenced in 04-ux-findings.md Method'; those scripts/artifacts (ux/, ux_results.json, journey/affordance/census/axe drivers) are not present anywhere in the repo.
- [REFS] Work (a) specifies the row-walker emits VERIFIED/FAILED/NOT-TRIGGERABLE (3 verdicts), but the DoD points at features/01's 'Live validation results' table as 'the verdict-table output format to produce'; that recorded table uses 6 verdicts (VERIFIED, DEAD-CONFIRMED, PARTIAL, NOT-TRIGGERABLE, NOT-TESTED, CODE-ONLY), so the specified 3-verdict output cannot reproduce that table.
- [AMBIG] 'the row-walker on features/01 must reproduce the validation results already recorded': unclear whether 'green' means each row's verdict must equal the recorded verdict (including PARTIAL/DEAD-CONFIRMED/CODE-ONLY, which the 3-verdict walker cannot emit) or merely that no recorded-VERIFIED row regresses to FAILED.
- [AMBIG] Layer (b) census differ and (c) behavioral differ are inherently old-vs-new, but T12's self-test runs against the CURRENT legacy pages only (no new page exists yet); it is ambiguous what the differ compares at T12 time (self-vs-self, or against the recorded 33-affordance census).
- [AMBIG] 'gated to UI-touching PRs' - no definition of which file globs/paths count as UI-touching for the CI gate.
- [DODISS] 'row-walker on features/01 must reproduce the validation results already recorded' is not mechanically checkable: the walker's 3-verdict output vocabulary cannot match the 6-verdict recorded table, and no numeric/row-level pass criterion is defined.
- [DODISS] 'tooling runs green' for the census differ (b) and behavioral differ (c) is undefined at T12 time because those layers require an old-vs-new page pair and only the current legacy pages exist during T12's self-test.
- [DODISS] 'documented invocation in ui/README.md' is checkable (README exists), but 'runs green ... headless in CI (gated to UI-touching PRs)' cannot be verified within this ticket if CI wiring for such gating is not part of T12's scope/deps.

## T13 impl=partial dod=no
- [MISSING] The T38 'always-visible switch' contract: T13's Work and DoD require injecting the switch, serving under the 'new-UI path (T38 routing)', and a 'switch round-trip keeps the trace loaded' DoD check, but T38 ('Dual-UI raw switch') lives in chunk-2-viewer.md, outside this chunk-1 file and its permitted reading scope. The switch/routing mechanism T13 builds on is not available or specified in chunk-1 context.
- [MISSING] Which concrete 'existing flamegraph export tests' must stay green (file names under dial9-viewer/ui/test_*.js) is not identified, so the DoD item can be run but the target set is unspecified.
- [MISSING] Exact new-UI URL/path scheme for the migrated page (owned by T38) is undefined, yet T13 must serve the page there and keep the legacy page at the canonical URL simultaneously.
- [REFS] 'Owns: features/03 ALL rows (A1..O-last)' — the inventory rows are actually numbered F1..F165; A-O are section headings, not row-ID prefixes. There is no 'A1' or 'O-last' identifier in 03-flamegraph-html.md, so the ID scheme cited does not match the file.
- [REFS] Deps line lists 'T38' as if it were a peer chunk-1 dependency; T38 is a chunk-2 ticket (chunk-2-viewer.md:433), a forward/cross-chunk dependency that inverts the stated chunk-1 'foundation, concept-independent' framing and the 'pipeline proof / first page' goal.
- [AMBIG] '165 rows' vs the section-based 'every section A-O' vs 'ALL rows (A1..O-last)': it is unclear whether ownership/parity is tracked per F-numbered row (F1..F165) or per section, which changes what the row-walker must enumerate.
- [AMBIG] 'legacy page STAYS servable at the canonical URL with the T38 switch injected; both carry the always-visible switch' — ambiguous whether T13 must modify the frozen/legacy flamegraph.html to inject the switch (seemingly editing a page whose modules are frozen core) or whether the switch injection is purely T38's responsibility.
- [DODISS] 'bundle within N6' — N6 defines the budget as at/below the page's current total JS payload and 'minified comfortably under it'; 'comfortably under' has no numeric threshold, so pass/fail is not mechanically decidable.
- [DODISS] 'affordance census diff old-vs-new empty (or ledger-justified: +switch control)' — the escape hatch 'or ledger-justified' makes it non-binary; any non-empty diff can be justified, so it is not a hard mechanical check.
- [DODISS] 'switch round-trip keeps the trace loaded' depends on the T38 switch existing; until T38 lands the check cannot be executed, so it is not mechanically verifiable within this ticket's own scope.

## T14 impl=partial dod=no
- [MISSING] The T38 'Dual-UI raw switch' spec: what the always-visible switch control is, how the 'new-UI path'/routing works, and what 'switch round-trip keeps context' requires. T38 lives in chunk-2-viewer.md (not the foundation chunk) and is only available to a cold-start agent as a title, but T14's Work and DoD (switch injection, switch round-trip, census '+switch control') hinge on its concrete behavior.
- [MISSING] The Tokio Stats (#570) and landing-page URL-state (#585) inventory rows that T14 claims to own: they do not exist in features/01-index-html.md yet (grep confirms none). Their concrete behavior/access paths are only produced by T01, so the acceptance baseline for the rows T14 owns is not readable from the referenced doc.
- [MISSING] The precise #585 landing-page URL-state contract to 'preserve' — no inventory row or param list describes what URL state the index page carries or restores.
- [MISSING] The J6 journey script used by the behavioral differ DoD item — J6 is only named ('S3 browse') in 04-ux-findings.md; the actual driveable journey steps are not specified in any referenced location.
- [REFS] 'features/01 ALL rows (A-I + T01 additions)' and 'T01 will have added the Tokio Stats + URL-state rows (#570/#585) - they are owned here too': features/01 currently contains no Tokio Stats or URL-state rows (verified by reading the file and grep). The rows T14 declares it owns are not present in the cited inventory; they are contingent on T01 landing first.
- [REFS] Dependency 'T38' is presented as a plain dep, but T38 is defined in chunk-2-viewer.md ('T38 - Dual-UI raw switch'), not in this foundation chunk. This contradicts the file header framing that chunk-1 tickets are concept-independent foundation and 'one landable PR' — T14 cannot land its DoD (switch control, switch round-trip) without a chunk-2 ticket.
- [AMBIG] DoD 'row-walker on features/01 green (matching or exceeding the recorded validation verdicts)': many recorded verdicts are NOT-TRIGGERABLE, NOT-TESTED, or CODE-ONLY (e.g. F5/F7/F8/F9 gaps, H4 cap, C7). It is unclear whether the new page must make these VERIFIED, or merely preserve the same untestable status — two readings that change the amount of work (e.g. synthesizing multi-segment traces).
- [AMBIG] 'the four amendment targets listed in T15 ... are ported as-is here': G8 is a DEAD affordance. 'Port as-is' could mean reproduce the dead/unwired sortable-header markup faithfully, or omit the non-functional handler — both defensible and observably different in the census diff.
- [DODISS] 'row-walker on features/01 green (matching or exceeding the recorded validation verdicts, NOT-TRIGGERABLE rows explicitly listed)': 'matching or exceeding' is not mechanically checkable for non-VERIFIED verdicts (NOT-TRIGGERABLE/NOT-TESTED/CODE-ONLY) — no objective pass/fail for 'exceeding'.
- [DODISS] 'census diff empty (+switch control, ledger)': not mechanically checkable in isolation because the expected '+switch control' delta depends on T38's unspecified switch, and 'ledger' has no defined location/format in this ticket.
- [DODISS] 'behavioral differ green on J6 journey': depends on a J6 journey script that is not defined anywhere reachable from the ticket, so the check cannot be run as written until that artifact exists (T12).

## T15 impl=partial dod=yes
- [MISSING] For the bucket-filter fix: the name/source of the 'server-config' predicate. The current filter is client-side (renderBucketPicker in index.html:490-518); the ticket says 'server-config/query-param predicate' but does not name a config key, say whether a new /api/config field (Rust server change) must be added, or define the query-param name/format.
- [MISSING] The exact ledger-entry format expected in this file's ownership summary (the summary currently only has a one-line 'T15 (amended rows G8, C6, I2, F10-axis)' entry; no ledger template is shown).
- [AMBIG] Work item 3 'bucket filter becomes server-config/query-param predicate' — server config vs URL query param are two materially different implementations (one needs backend work, the other is client-only); 'with current behavior as default' fixes the default but not the mechanism.
- [AMBIG] 'unknown-layout keys render raw (no shifted columns)' — 'render raw' could mean display the full key string verbatim, or render the recognized fields while blanking the ambiguous ones; the display treatment of an unknown-layout key is not specified.
- [DODISS] 'per-fix Vitest coverage' has no coverage threshold or explicit per-fix test list, so 'covered' is checkable only as 'a test exists per fix', not as a hard metric.
- [DODISS] 'ledger entries recorded in this file's ownership summary' — checkable that the summary was edited, but with no defined ledger format there is no mechanical pass/fail criterion for correctness.

## T16 impl=partial dod=no
- [MISSING] Whether the frozen core's ParsedTrace output is structured-clone-safe. The ticket asserts 'structured-clone the ParsedTrace to the store' but nothing in the referenced docs confirms the output contains only cloneable data (no functions/class instances/prototype methods). 03 F4 mentions BigInts and polymorphic event objects; whether these survive structuredClone unchanged is unverified and is load-bearing for the DoD's 'identical output / deep-equal' check.
- [MISSING] The store implementation (T07) is not a declared dependency, yet the Work item requires delivering the ParsedTrace 'to the store's trace slice'. Where/how the trace slice is written (and whether that wiring is even in T16's scope, since the DoD never tests it) is unresolved.
- [MISSING] How to execute/test an actual Web Worker under Vitest's default node environment. The DoD requires 'parsing the demo trace via the worker path' in Vitest, but node env has no browser Worker; whether to use @vitest/web-worker, jsdom, or worker_threads, and whether the test drives a real worker or just the orchestration, is unspecified.
- [MISSING] Physical placement of the worker entry file. The 2.1 directory layout has no workers/ directory and 2.8 only says parsing happens 'in the Web Worker' without naming a location.
- [REFS] Ticket cites architecture '2.8 (worker placement)', but 2.8 gives no worker placement detail beyond the phrase 'in the Web Worker, section 1 N-notes' - it does not specify a file or directory for the worker entry.
- [AMBIG] 'fetch (main or worker)' leaves undecided whether the network fetch runs on the main thread or inside the worker. This changes where AbortController lives and how progress is reported, materially affecting the implementation.
- [AMBIG] 'structured-clone the ParsedTrace to the store's trace slice' vs. a DoD that only tests parse-output equality/progress/abort: unclear whether store integration is actually in scope for T16 or deferred to a page/chunk-2 ticket.
- [AMBIG] 'via the worker path' in the DoD could mean a real spawned Worker thread or merely the load.ts orchestration path exercised in-process - the two readings need different test infrastructure.
- [DODISS] 'abort mid-parse leaves no dangling state' - 'dangling state' is not defined, so there is no mechanical pass/fail criterion (no listener/handle/store-key/pending-promise checklist to assert against).
- [DODISS] 'identical output to direct parse (deep-equal on counts + spot samples)' - which counts and which spot samples is unspecified; a reviewer cannot mechanically confirm the intended coverage.
- [DODISS] 'progress events observed' - no specification of which progress events, their expected sequence, or minimum count, so 'observed' is not a concrete check.

## T17 impl=partial dod=no
- [MISSING] Concrete budget constant values: N19/2.8 gives only a range for resident parsed/raw bytes (~100-150 MB) and says the raw-gzip-bytes cache has 'its own budget'/'a separate larger budget' with NO number. The DoD requires 'eviction below hard budget' and 'under budget' tests, which need concrete constants to assert against.
- [MISSING] Which encoder to use for 'synthesized segment fixtures via the encoder' - the DoD names 'the encoder' but no path/module is given for producing multi-segment fixtures; only the demo-trace-repeat alternative is concretely available.
- [MISSING] How 'navigates end-to-end' is exercised without a viewer/UI (explicitly out of scope): the ticket needs a programmatic viewport-driver harness but does not point to one or define its interface.
- [AMBIG] Context says '2.8 ... ALL five hard edges - each is a DoD item', but the DoD marks the fifth (aggregate coverage) as 'coverage fallback n/a here' - so it is simultaneously claimed as a required DoD item and declared not-applicable (it actually belongs to T18). Unclear whether a coverage test is expected here.
- [AMBIG] The 'Feedback + hysteresis' hard edge bundles two things: status-bar progress (UI, out of scope per 'minimap/UI' exclusion and chunk 2) and eviction hysteresis. The DoD only tests eviction-below-budget; whether any feedback surfacing is required in T17 is ambiguous.
- [AMBIG] 'budget hysteresis' / 'eviction triggers below the hard budget since GC lags eviction' - the size of the hysteresis margin below the hard budget is unspecified, changing where the eviction threshold is set.
- [DODISS] 'coverage fallback n/a here' is not a checkable test - it is an explicit non-item, so it cannot be mechanically verified as done (and conflicts with the 'each is a DoD item' framing).
- [DODISS] 'simulated multi-segment scenario ... navigates end-to-end under budget' is not mechanically checkable as written: 'navigates end-to-end' has no defined driver/assertion given UI is out of scope, and 'under budget' has no concrete numeric threshold to check against.
- [DODISS] 'boundary poll marked truncated not long' is checkable only if the truncated-vs-long-poll distinction is observable in the parsed output; the ticket asserts markers are 'surfaced in the parsed output' but does not define the field/shape to assert on.

## T18 impl=yes dod=no
- [MISSING] How to bring up the dev-server with aggregation enabled to capture real fixtures: `aggregation_enabled` becomes true only when `state.agg.is_some()` OR `allow_byo_creds` (an S3 source / AggContext). The ticket says 'capture real responses from the dev-server with aggregation on' but gives no flags/env/source setup for running the dev-server in that mode, and neither /api/tokio-stats nor /api/flamegraph returns real aggregate data without an S3 source folded to Parquet.
- [MISSING] The exact fallback target for 'fall back to listing metadata' — the listing-metadata source (segment extents) lives in segments.ts / T06 scope, not this ticket; which listing-metadata API/type aggregates.ts should degrade to is not pinned down.
- [REFS] DoD requires testing a '503' degradation path, but the referenced endpoints never emit 503 — error returns are NOT_FOUND (404) at tokio_stats.rs:81,113 and flamegraph.rs:204,278, plus INTERNAL_SERVER_ERROR (500) at flamegraph.rs:296. There is no SERVICE_UNAVAILABLE path to record as a fixture or reproduce.
- [AMBIG] 'graceful degradation when aggregation_enabled=false' — unclear whether the client should skip calling the endpoints entirely (gate on /api/config first) or call them and handle the error/empty response; both are reasonable and produce different control flow.
- [AMBIG] 'coverage partial' handling — Coverage is an Option (skipped when None) in both responses; the ticket does not define what shape/threshold of Coverage counts as 'partial' vs complete, so the degradation trigger is open to interpretation.
- [DODISS] 'degradation paths tested (503, ...)' is not satisfiable: no server code produces 503, so a 503 test cannot be derived from real responses as the DoD's own 'capture real responses' clause requires.
- [DODISS] 'capture real responses from the dev-server with aggregation on' is not mechanically verifiable without a specified way to enable aggregation and provide a data source; the DoD gives no concrete command/environment to make this reproducible.

## T19 impl=partial dod=no
- [MISSING] The actual flamegraph zoom-state URL param names. The Work item requires unifying existing flamegraph zoom-state params through the codec 'without breaking old params', and the DoD requires old-format URLs to still resolve, but neither N10 nor T13 enumerates these param names (N10 in 02-architecture.md just says 'zoom-state params on the flamegraph page'). You'd have to reverse-engineer them from frozen-core flamegraph.js, which the ticket doesn't point at.
- [MISSING] The concrete slice->URL-key mapping / hash schema. The ticket says 'store slices (T07) are the source of truth', but T07 defines only the generic store mechanism (update/subscribe/derived) and explicitly 'Owns: none'; it does not define which slices carry viewport/selection/POI/time-mode or their shapes. Viewer-page slices are explicitly deferred to chunk-2 (pending concept pick), so what the codec serializes on the viewer is undefined at this ticket's landing time.
- [MISSING] Definition/availability of the 'behavioral differ' the DoD relies on. It is referenced as if it exists but is not introduced by T19's stated deps (T07, T13); it appears to come from the T12 parity tooling, which is not a direct dependency.
- [REFS] 'inventory N10 param list' (DoD) and 'N10' (Context) are ambiguous/mislabeled: the conventions define 'Inventory' as features/01-03. Under that definition N10 resolves to features/02-viewer-html.md:277 'N10. Polling sections', which is NOT a param list. The intended reference is 02-architecture.md:68 'N10. URL contract stability' — a different document not covered by the 'Inventory' shorthand.
- [REFS] Goal lists URL state as 'viewport/selection/POI/time mode', but the cited ADR section 9 and finding S3 describe 'viewport/selection/POI' only — 'time mode' is added by the ticket and is not backed by the referenced sources.
- [AMBIG] 'restore-on-load integration test per page' / 'Applies to all three pages where slices exist': at T19's landing time only the flamegraph page exists on the new stack (T13); index (T14) has no slices per the ownership summary and viewer is chunk-2. So it's unclear whether 'per page' means one page now or is a forward-looking requirement, which changes test scope.
- [AMBIG] 'copy-link action' plus 'a minimal toolbar button ships here': whether the button must be wired into the migrated flamegraph page (T13) chrome specifically, versus a standalone control, is unspecified — affects where/how it integrates.
- [DODISS] 'restore-on-load integration test per page' is not mechanically checkable without a defined page count — with only one consuming page (flamegraph) landing in chunk 1, 'per page' has no fixed target to verify against.
- [DODISS] 'old-format URLs (inventory N10 param list) still resolve' cannot be executed as written because the referenced 'N10 param list' does not enumerate concrete param names (and 'inventory' N10 points to the wrong row); the set of old URLs to test is undefined.
- [DODISS] 'behavioral differ confirms legacy links unchanged' is not self-contained: the differ tool is not part of T19's declared deps, so whether it's available/how it's invoked to check this is not mechanically determinable from the ticket.

## T20 impl=partial dod=no
- [MISSING] Which help overlay the migrated chunk-1 pages actually extend: T20's Work says the unified help overlay 'extends the existing overlay content, features/02 T', but features/02 is the viewer page (chunk-1 header lines 23-25 assign it to chunk 2) and T20 does not migrate it. The concrete overlay to extend on the pages T20 lands on (flamegraph = features/03 F12; index/browser) is never identified.
- [MISSING] Which keys have a real target on each migrated page. T20 lands only on flamegraph (T13) and index/browser (T14), but the vocabulary (n/p POI step, g goto-time, WASD timeline nav, f fit, z zoom-undo) is timeline/viewer-centric. Flamegraph has zoom but no POIs/timeline pan; the browser page is an S3 heatmap, not a timeline. The ticket says 'page-specific integrations land on migrated pages only' but never states which of / n p g f z WASD wire to a surface on flamegraph vs index.
- [MISSING] The search palette is specified to source tasks/spans/POIs 'via lib/trace/query' (T09), but T09 is not in T20's dependency list (Deps: T07, T13/T14, T12). It is only satisfied transitively because T13/T14 depend on T05-T09; the query API contract T20 relies on is not directly linked.
- [REFS] 'extends the existing overlay content, features/02 T' (Work) and DoD 'row-walker on the affected inventory rows (features/02 T help overlay...)': features/02 T is the viewer page's help overlay. The viewer is not migrated in chunk 1, so the overlay T20 actually changes lives on features/03 (flamegraph F12) and the browser page, not features/02. Verified: features/02 header/ownership (this file lines 23-25) places features/02 rows in chunk 2.
- [REFS] Goal says the priorities are 'K1-K3', but 'Owns' claims 'K1/K3/K4/K5'. These sets disagree: K2 (browser keyboard-dead, task-blocking, 04 line 73) is named in the goal range but not in Owns and is not addressed by the mechanism-only work, while K4/K5 are owned but outside the stated K1-K3 goal.
- [AMBIG] 'existing bindings unchanged' vs adding '?' help and WASD/arrow nav to the flamegraph page, where 04 K3/K7 record that '?', h, F1 and arrow keys are all currently dead. Adding a binding where none existed could be read as either 'unchanged' (nothing removed) or a change to that page's contract; which pages' bindings count as 'existing' is unspecified.
- [AMBIG] 'row-walker on the affected inventory rows (features/02 T help overlay, H11/H12 arrows unchanged)': whether the row-walker runs against the (unmigrated, legacy) viewer page to prove those rows are untouched, or against the migrated flamegraph/index pages, is ambiguous - the two readings target different pages and different verification work.
- [AMBIG] WASD key semantics are not given in the ticket ('WASD nav'); the mock keyboard.html defines W=zoom in, S=zoom out, A=pan left, D=pan right. Resolvable via the mock, but the ticket text alone is under-specified.
- [DODISS] 'row-walker on the affected inventory rows (features/02 T help overlay, H11/H12 arrows unchanged)' is not mechanically checkable as written: features/02 (viewer) is a legacy, unmigrated page in chunk 1, so it is unclear which page URL the row-walker drives and what 'affected' means for a ticket that lands only on flamegraph/index. The rows named belong to a page T20 does not touch.
- [DODISS] 'axe scan clean on palette/overlay (T12 tooling)' and 'row-walker ...' both depend on T12 tooling and on there being a migrated page hosting the palette/overlay; checkability is contingent on which migrated page actually renders the palette, which the ticket does not pin down.
- [DODISS] 'K3 verified: ? opens help on every migrated page' is checkable in principle, but 'every migrated page' resolves to only flamegraph + index in chunk 1; the item reads as broader coverage than the ticket scopes, making the pass/fail boundary unclear.

## T21 impl=partial dod=no
- [MISSING] Which concrete W rows T21 owns: the parenthetical '(timezone mode, esc cascade, escaping)' does not map to the inventory's actual W1-W12 labels, and W5/W6/W8/W9 are cross-cutting mechanisms the chunk-1 header assigns to T16-T20 ('cross-cutting mechanisms only'). The exact T21-owned W-row set needed to run the DoD row-walker is undefined.
- [MISSING] The specific micro-templating library for DOM components: arch 2.4 names only a 'lit-html class' of library, not a concrete dependency, and no linked ticket/dep establishes/installs it, yet T21 must build declarative mount(el,store) components with it.
- [MISSING] How the T12 'row-walker' marks an owned row 'green' — the DoD hinges on it but T21 links to no T12 definition of the green criterion (T12 is only listed as a dep).
- [REFS] Owns line 'W rows (timezone mode, esc cascade, escaping)': 'escaping' is A17 (an A-section row, separately owned by T21's A rows, not a W row) and 'esc cascade' is D9 (section D), which the chunk-2 ownership summary assigns to T33 (C,D,E -> T33). Neither is a W row; only 'timezone mode' loosely maps to W1.
- [REFS] DoD names 'TZ/format toggles' and 'esc cascade' as T21-owned rows expected green in the walker, but the TZ/format toggle controls are E1/E2 (section E) and esc ordering is D9 (section D) — both owned by T33 per the ownership summary, conflicting with T21's DoD.
- [REFS] 'minus T23's named W rows' / T23's 'W keyboard rows': the inventory's W section (W1-W12) contains no keyboard-specific row, so the subtracted subset does not exist as labeled rows.
- [AMBIG] T21/T33 boundary on time formatting and escape handling: does T21 own the W1 cross-cutting time-format behavior while T33 owns the E1/E2 toggle buttons and D9 esc ordering? The split is unstated and changes what T21 implements and what its row-walker must cover.
- [AMBIG] 'shell renders with placeholder tracks' — how many placeholder tracks, and what a placeholder must contain to satisfy the DoD, is unspecified (and overlaps T36 track-management, listed out of scope).
- [AMBIG] Micro-templating library is described as a 'class' of library (2.4), not a concrete pick, so two implementers could choose different libraries.
- [DODISS] 'owned rows green in the T12 row-walker (help overlay, toasts, TZ/format toggles, esc cascade)': not mechanically checkable because the owned-row set is ambiguous and the named examples (TZ/format toggles = E1/E2, esc cascade = D9) fall in sections owned by T33, so which rows must be green under T21 is undetermined.
- [DODISS] 'shell renders with placeholder tracks on the demo trace': 'placeholder tracks' is subjective/undefined, so pass/fail is not mechanical.

## T22 impl=yes dod=no
- [MISSING] No pass/fail threshold for 'stroke self-time bounded' — the DoD only says to document the measured number, so there is no target value to verify the port against (the F1 baseline of 89.0s/76% is the problem number, not an accept bound).
- [MISSING] The concrete frame-budget number behind the DoD's 'no long task > 1 frame from lanes' — neither N2 nor the ticket gives the frame duration (e.g. 16ms) or how a 'long task from lanes' is attributed/measured by the T12 perf probe.
- [REFS] DoD cites 'meets N2 (no long task > 1 frame from lanes)'. N2 (02-architecture.md:24-27) is about render *cadence* — at most one full render per animation frame and no synchronous full render on mousemove — it says nothing about a single render/long-task staying under one frame. A coalesced render can still exceed a frame (that is exactly the F1/F2 860ms problem). The parenthetical restates N2 as a different, stronger requirement.
- [AMBIG] Context says section G is 'G1-G20' while Owns says 'G1-G19 (G20 retired)'. G20 is DEAD per the inventory and dropped via ledger, but the two ranges in the same ticket read inconsistently on first pass.
- [AMBIG] 'stroke self-time bounded (document the measured number in the PR)' can be read as (a) just record whatever number results, or (b) meet some implied bound — these imply different acceptance behavior for the reviewer.
- [DODISS] 'stroke self-time bounded' is not mechanically checkable — no threshold is given; only 'document the measured number' is verifiable, 'bounded' has no defined pass criterion.
- [DODISS] 'perf: pan storm on x8 demo meets N2 (no long task > 1 frame from lanes)' — N2 as written does not define a long-task/frame-budget criterion, so 'meets N2' cannot be checked against the cited NFR; the intended metric and threshold are unspecified.

## T23 impl=partial dod=no
- [MISSING] Which specific inventory rows constitute the 'W keyboard rows' that T23 owns: section W (W1-W12 in features/02-viewer-html.md) contains no keyboard rows at all (they are time formatting, coloring, render pipeline, LOD, parsing, analysis, formatting utils, flamegraph export, creds, state reset, URL params, layout constants). The owned scope-from-W is therefore undefined.
- [MISSING] Which behavioral-differ journey IDs define 'pass' for the pan/zoom/select DoD item. Sibling ticket T22 names concrete journeys (J2/J4); T23 says only 'pan/zoom/select journeys' with no IDs, so there is no fixture set to diff against.
- [REFS] Context (line 92-93) calls 'keyboard-selection state machine, ARIA announcements (A16)' *W rows*, but A16 is a section-A row (A16. ARIA live region, features/02:45) and the keyboard-selection state machine is row H9 in section H (features/02:168). No row in section W concerns keyboard or ARIA.
- [REFS] T23 claims A16 (ARIA announcements) in Context/Owns, but the chunk-2 ownership summary maps 'A -> T21' and T21's Owns line explicitly claims 'features/02 A1-A18' (which includes A16). A16 is thus assigned to two tickets.
- [AMBIG] A16 (ARIA live region) is owned by both T21 (Owns: A1-A18) and T23 (Context: 'ARIA announcements (A16)'); unclear which ticket lands/tests it.
- [AMBIG] K4/K5 ownership is split: T20 'Owns the keyboard mechanism + K1/K3/K4/K5 findings' while T23 lists K4/K5 as its Amendments. T20 defers 'page-specific integrations' to chunk 2, but the boundary between T20's mechanism and T23's viewer integration for zoom-history/WASD is not drawn.
- [AMBIG] Row H7 (Shift+drag region select) both defines the drag interaction (T23) and opens the flamegraph/blocking/heap panel by data present (section S = T32). The ticket claims all of H1-H13 but the split between the region-select input machine (T23) and the region analysis it triggers (T32) is unspecified.
- [DODISS] 'behavioral differ on pan/zoom/select journeys vs legacy' names no journey IDs, so the pass/fail set is not mechanically determined (contrast T22, which cites J2/J4).
- [DODISS] 'row-walker green on H rows' is checkable, but the ticket also claims ownership of 'W keyboard rows' with no such rows existing, so completeness of the owned-row walk cannot be verified as written.

## T24 impl=partial dod=no
- [MISSING] The store contract for the at-cursor readout that T24 must write and T31's inspector consumes: T24 says the info readout 'writes into the inspector panel via store', but the inspector is built by T31 (which depends on T24, not vice-versa), and no store key/shape for this readout is specified anywhere T24 links to. T24 must define it blind.
- [MISSING] The definition/API of the store `transient` slice the overlay RAF channel is supposed to use — referenced as if it exists, but T24's only store dep is T07 (title-only), and neither T24 nor its readable context specifies whether the transient slice already exists or must be created here, nor its shape.
- [REFS] DoD/Context cite 'N4' for 'crosshair alignment pixel-exact vs lanes', but 02-architecture N4 (Rendering fidelity) defines the shared time-axis alignment invariant across timeline/lanes/panels (A13, panel_layout.js) — it does not address crosshair-to-lane alignment. The mapping is an extrapolation, not what N4 states.
- [REFS] Context maps amendment F7 to 'persistent at-cursor surface in inspector', but 04-ux-findings F7 is specifically 'No persistent "what is selected" status or explicit clear/close affordance on selection surfaces' — the at-cursor stats scattering is actually finding S4, not F7. F7's own subject (selection status + clear affordance) is also claimed by T31, so the at-cursor reading of F7 is a stretch.
- [AMBIG] Lane hover tooltip ownership seam: T24 owns the V (tooltip system) rows and calls G16 the 'tooltip content contract', but G16 (Lane hover tooltip) is owned by T22 (G1-G19). It is unclear which ticket implements the lane tooltip's content assembly/innerHTML — T24 building the full lane tooltip from T22-supplied data, vs T22 building it using T24's placement primitive. This changes what code lands in T24.
- [AMBIG] F7 is split 'partial' between T24 (at-cursor surface in inspector) and T31 (selection status + clear affordance), both landing in the same inspector panel, with no crisp boundary between the at-cursor readout and the selection-status line — risk of overlap or a gap.
- [DODISS] The DoD has no acceptance item for the ticket's stated inspector integration (Work + Owns 'F7 partial: persistent at-cursor surface in inspector' / 'at-cursor stats ALSO mirror into the inspector'). All three DoD items cover only row-walker, the F3 forced-layout probe, and crosshair alignment — the inspector mirroring is never mechanically verified.
- [DODISS] 'crosshair alignment pixel-exact vs lanes (N4)' has no defined verification method (visual check vs Vitest on layout math vs tolerance). Contrast T25, which explicitly specifies 'Vitest on layout math + one visual check in T12'; T24 leaves how 'pixel-exact' is asserted unstated.

## T25 impl=partial dod=no
- [MISSING] The TZ/format-mode state/interface the axis must consume: T25's goal says 'ticks, TZ/format modes', but the TZ/timezone-mode toggle is owned by T21 (its W rows) and the F1 inventory row only says 'fmtTs-formatted labels'. Nothing in T25's provided context specifies how the axis reads current TZ/format state (the API surface T21 exposes), which is needed to render mode-correct labels.
- [MISSING] The concrete definition of the 'viewer-axis date qualification' amendment: it is described only as a 'mirror of the T15 heatmap-axis amendment' but there is no numbered 04-ux-findings row for it, so the exact rule (e.g. what counts as 'crosses a day boundary', which TZ the boundary is evaluated in, label format) is defined only by analogy to T15's features/01 heatmap fix.
- [REFS] Context cites 'N4 (axis alignment across all tracks via T08 layout)'. N4 in 02-architecture.md is titled 'Rendering fidelity' (DPR scaling, scrollbar compensation) and only mentions the time-axis alignment invariant as one clause tied to '02 A13, panel_layout.js' — it is not primarily an axis-alignment item, so the paraphrase is loose.
- [REFS] Context attributes the date-qualification amendment to 'issue #137 family'; the only #137 reference in 04-ux-findings is row S2, which is about a missing go-to-timestamp control and a hidden TZ toggle, not about rendering date+time on day-crossing spans. The linked friction row does not describe the amendment the ticket assigns.
- [AMBIG] 'TZ/format modes' in the Goal: unclear whether T25 must implement the mode toggles/state (they appear to be owned by T21's W rows) or merely render the axis according to a mode owned elsewhere. The two readings change scope substantially.
- [AMBIG] 'F rows' / 'F (F1-F...)': the F range is written truncated. Section F contains only F1 and F2 (F3-F19 under the same file are the separate Flamegraph section S). A reader could misinterpret the truncation as more rows existing.
- [DODISS] 'one visual check in T12' is not mechanically checkable as written — a visual check implies human/subjective verification rather than a pass/fail assertion.
- [DODISS] The owned amendment ('viewer-axis date qualification' / date+time on day-crossing spans) has no corresponding DoD item; the DoD only covers row-walker green and alignment math, so completion of the amendment cannot be mechanically confirmed (relies on the implicit contract-amendment rule updating the F1 row).

## T26 impl=partial dod=yes
- [MISSING] Under the layout decision (unified track column + persistent inspector sidebar, T31 owns sections P/Q/R), it is unspecified whether the focused-span metadata surface (J3: 184px left label area with span name + key/value field rows; J4: per-row copy buttons) renders inside the spans track or migrates into the T31 inspector. T26 'owns J rows' but the old J3/J4 'panel label area' conflicts with the sidebar-as-one-surface direction, and T31's context does not claim span metadata.
- [MISSING] The DoD's '<= 1 render/frame (T12 probe)' relies on a render-per-frame perf probe, but T12's Work/DoD only defines a row-walker, affordance census, behavioral differ, and axe/contrast scan — no perf/render-count probe. The instrument the DoD invokes is not a defined T12 deliverable (T22/T23/T24 also assume a 'T12 perf probe' that T12 does not enumerate).
- [AMBIG] 'span focus behavioral-diffed vs legacy on J4 journey': the 'J4' here is journey J4 ('follow a task', per 04-ux-findings.md journey list), NOT feature-row J4 ('Metadata copy button') in the section T26 owns. Since T26 owns section-J feature rows J1-J14 AND references 'J4 journey', the shared J-prefix is a real collision; resolvable only by knowing the 04-ux journey catalog. (Span focus itself is feature-row J2, not J4.)
- [AMBIG] 'Span panel as a time-aligned track' plus 'span focus chain': whether the click-select side effect of J2 (selecting the task polling at span start / dimming other tracks) is owned/implemented here or is deferred to the S4 cross-track re-scoping owned by T31 is not delineated.

## T27 impl=partial dod=no
- [MISSING] Whether T27 renders the custom-event hover guide line (I4) and selected-event pin marker (I5) itself, or only dispatches store state for T24 to render them. The ticket goal lists 'click-to-pin' and 'hover guide line', but I4/I5 are owned by T24 (features/02 I1-I6), and T24 is not in T27's deps (T21, T07, T09).
- [MISSING] The sidebar Event/Related tabs that K4 populates (and that the DoD's 'sidebar KV behavioral-diff' checks) are owned by T31 (P/Q/R rows). T31 is not a declared dep, so it is unclear whether T27 must have T31's sidebar available to run/verify that DoD item, or only assert the dispatch.
- [MISSING] What '03 H8' designates for the bounded-scan requirement (see inaccurateRefs) - the correct source rule for binary-search-over-sorted-events is not unambiguously locatable.
- [REFS] '03 F5/H8': the '/H8' component is unresolvable/incorrect for a bounded-scan-over-events rule. 03-performance-findings.md has only F1-F7 (no H8). F5 does cover the custom-events O(N) visibility scan (viewer.html:3394-3398), so F5 is right; H8 does not. The only H8 in features/02 is section H's 'Alt/Option+drag zoom' (unrelated), and chunk-3 references 'hypotheses H1-H8' under F4's data-model/heap sweep (also unrelated to event scanning). The binary-search-helper rule is actually F6 (lib/trace/query).
- [REFS] Goal 'hover guide line' and 'click-to-pin' describe the orange guide (I4) and the persistent selected-event marker (I5), which the inventory assigns to section I and which T24 explicitly owns (I1-I6); they are not features/02 section K rows that T27 'Owns'.
- [AMBIG] 'click-to-pin ... behavioral-diffed': ambiguous whether T27 implements the pin marker rendering (I5, owned by T24) or only the K4 click action that sets selection state; the two readings put the marker-drawing code in different tickets.
- [AMBIG] 'sidebar KV behavioral-diffed vs legacy': ambiguous which sidebar surface/tabs (Event vs Related, owned by T31) and which fields constitute the 'KV' to diff, and whether the diff runs against a built T31 sidebar or a stub.
- [DODISS] 'click-to-pin + sidebar KV behavioral-diffed vs legacy' is not mechanically checkable as written: it depends on the pin marker (T24/I5) and the sidebar Event/Related tabs (T31/P-Q-R), neither of which is a declared dependency, and no specific legacy journey/fixture is named for the diff.
- [DODISS] 'row-walker green on K' is checkable via T12 tooling, but K rows include K8 (status DEAD/not-implemented) and K7 (internal task-resolution, no UI); the DoD does not state the expected walker outcome for a DEAD/internal row, so 'green on K' is under-specified for those rows.

## T28 impl=partial dod=no
- [MISSING] T08 stroke-batcher API/usage contract: the ticket mandates 'stroke batching for the curve (03 F1)' and lists T08 as a dep, but neither T28 nor the linked 03 F1 finding describes T08's actual interface (per-column vertices, one-path-per-style batcher signature). 03 F1 only states the design rule, not the API to call.
- [MISSING] Definition of 'row-walker green' / the T12 parity tooling: the DoD's pass criterion depends on T12 mechanics not described in T28 or its one-hop links (this is a chunk-wide convention but the concrete acceptance mechanism is unspecified here).
- [MISSING] How the CPU 'panel' becomes a 'track' under the unified-column layout decision: T28 says 'Process CPU panel as a track' but the track container/host contract lives in T21 (dep), not restated; the collapse/height behavior (currently 92px expanded/24px collapsed foldable, features/02 L header) vs track model is unresolved in-ticket.
- [REFS] Goal/Context says 'stroke batching for the curve (03 F1)'. Per features/02 L1 the CPU panel is a stacked-area/bar chart drawn with fillRect bars (avg-cores-over-time bars); the only stroked primitives are the dashed orange capacity line and the 25/50/75% grid lines. There is no 'curve'. 03 F1's stroke-batching concern (step lines, dashed markers) is about lane/queue strokes, not this bar chart, so 'the curve' mischaracterizes what L actually renders and what needs batching.
- [AMBIG] 'stroke batching for the curve' vs the L1 reality of fillRect bars: an implementer cannot tell whether they must (a) re-render the existing bars unchanged and only batch the capacity/grid strokes, or (b) reshape the chart into an actual stroked curve. These are materially different implementations.
- [AMBIG] 'row-walker green on L' — L6 is documented as DEAD (no click handler wired). Whether a DEAD row must be reproduced (still DEAD) to count as 'green', or is excluded, is not stated and affects whether the CPU canvas click affordance must be intentionally left unwired.
- [DODISS] 'row-walker green on L' — mechanically checkable only via T12 tooling that is not defined within T28 or its one-hop links; as written it is not self-contained (relies on undescribed external gate).
- [DODISS] 'rendered values behavioral-diffed (avg/max readouts exact vs legacy)' — checkable in principle (L2 info label shows avg cores / avg% / max cores), but 'behavioral-diffed vs legacy' presumes a legacy-diff harness/fixture that T28 does not name or link, so the check is not mechanically runnable from the ticket alone.

## T29 impl=partial dod=no
- [MISSING] The 'ledger' where amendments/presentation changes must be documented (DoD: 'new presentation documented as ledger amendment'; 'amended rows updated in-diff') is never located or format-specified. The ticket says the contract-amendment rule was 'established in T15' but T15 is not readable, and no ledger file/section is discoverable under docs/ui-inventory or docs/adr. Need: which file holds the ledger and its entry format.
- [MISSING] Concrete steps/acceptance for the J7 'queue buildup' journey used by the behavioral-diff DoD. J7 is only a one-line label ('queue buildup') at 04-ux-findings.md:17; the actual journey walk is said to live in T12 tooling / session scratchpad, not in any referenced doc.
- [MISSING] How M7/M8 (drag-select spawned tasks -> list in sidebar with clickable task-id links) should be ported. These are owned M rows but the sidebar is T31's surface and taskFirstPoll data is required; neither the Work section nor Deps address them.
- [MISSING] How M6 hover info (which reuses I6 #info-panel, owned by T24) is satisfied here given T24 is not a dependency and the info readout is being moved into the inspector by T24.
- [REFS] Ticket describes section M as '(global/local series, fold summaries)', implying a narrow global+local strip, but section M actually contains interactive rows well beyond that: M3 active-task line (separate right y-axis), M7 drag-select-spawned-tasks feeding the sidebar, M8 spawned-task link click. The 'Owns: features/02 M rows' + 'row-walker green on M' DoD therefore covers substantially more than the ticket's Work/Goal describes.
- [REFS] The Goal frames local queue as only an 'in-lane sparkline + q:NN' (per S6), but M2 already renders a 'Max-local queue line' inside the queue panel itself, so a local-queue panel representation already exists; the ticket's 'split-brain' framing partly mis-states the current state for the panel it owns.
- [AMBIG] Ownership of the in-lane 'q:NN' legend entry: T22's context claims F3(lanes) 'legend covers q:NN and all in-lane marks', while T29 owns F3(queue) and says 'in-lane q:NN gains a legend entry (coordinated with T22)'. Which ticket actually adds the q:NN legend entry is unresolved by 'coordinated with'.
- [AMBIG] 'always-visible representation' (S6) vs T36's per-track collapse (section O mechanics): whether the queue track may still be collapsed like other tracks, or must be exempt from collapse, changes the implementation.
- [AMBIG] 'global + per-worker-max local' — whether 'active-task line' (M3, a third series on a separate right-side axis) must be carried into the new track or dropped is not stated, despite M3 being an owned row.
- [DODISS] 'J7 journey behavioral-diff: same numbers, new presentation documented as ledger amendment' — 'documented as ledger amendment' is not mechanically checkable because the ledger's location/format is undefined; 'same numbers' also depends on a J7 journey definition not present in referenced docs.
- [DODISS] 'a zero-global trace renders a visible baseline (Vitest on the scale function + visual check)' — the 'visual check' half is a manual/subjective step, not mechanically verifiable as written.
- [DODISS] 'row-walker green on M (amended rows updated in-diff)' is only checkable if the T12 row-walker exists and enumerates all M1-M9 rows including the interactive M7/M8/M3 rows the ticket's Work does not address; as written the DoD silently pulls in unspecified scope.

## T30 impl=partial dod=yes
- [MISSING] Which parts of section N (N1-N12) become a time-aligned 'track' vs. move into the persistent inspector: the ticket's defining framing is a 'track/inspector hybrid' but no mock or spec is linked that pins the split. The concept-1 mock is referenced only in the chunk header's global layout decision, not cited for task-detail, so the hybrid boundary is unresolved.
- [MISSING] The shared store mechanism for the G8/N8 waker-hover highlight coordination with T22: G8 uses legacy `hoveredWakerTaskId` on the lanes canvas (owned by T22), and N8 triggers it from the task-detail waker label (owned here). The ticket says 'coordinates with T22' but does not specify the store slice / contract by which T30 signals the highlight to T22-owned lanes.
- [MISSING] The store derived-cache / selection-slice API from T07/T09/T21 needed to implement 'derived cache keyed by selection slice' (only dependency titles were read); this is expected cross-ticket context but is load-bearing for the DoD's cache invalidation test.
- [REFS] Context cites '03 F5-H1'. Finding F5 exists and its mechanism catalog does describe renderTaskDetail re-collecting/re-sorting every poll per frame (viewer.html:4203-4209), matching the parenthetical. But there is no 'H1' in 03-performance-findings.md (it only has findings F1-F7); and features/02 section H1 is the 'Zoom In button' viewport row, unrelated to task detail. The '-H1' suffix has no valid referent and is misleading.
- [AMBIG] 'Per-task detail ... as a track/inspector hybrid per the layout decision' has two reasonable readings that change the build: (a) task detail becomes one time-aligned canvas track (like the legacy 160px panel) with only summary text mirrored to the inspector, or (b) it is split so lifetime/poll visuals are a track while N2/N3 header + N4/N11 flamegraph links live in the inspector. The N/T31 (inspector, P/Q/R) boundary is not drawn.
- [AMBIG] N4 ('idle flamegraph (N)') and N11 (async-stack sidebar flamegraph) open flamegraph/sidebar surfaces owned by T32/T31; whether T30 re-implements those launch actions or merely dispatches to those tickets' surfaces is unspecified.

## T31 impl=partial dod=no
- [MISSING] Handling of the two DEAD rows T31 owns is unspecified: P8 (sidebar width persistence, 'region intent not implemented', DEAD) and R6 (summary-row click has cursor:pointer+data-leaf but no handler, DEAD). Other tickets explicitly retire DEAD rows via ledger (e.g. T22's G20); T31 is silent on whether P8/R6 should be implemented, preserved-as-DEAD, or retired, so 'row-walker green on P/Q/R' has no defined target for them.
- [MISSING] The S4 DoD requires 'selecting a task re-scopes spans/events tracks (dimming)', but the spans track (T26) and events track (T27) are owned by other tickets and are NOT listed among T31's deps (T21, T07, T24). Which side owns/implements the dimming render in those tracks, and how T31 drives it, is not specified — T31 cannot satisfy this acceptance from its own owned surface alone.
- [AMBIG] 'resizable (P rows)' vs P8: P2 (drag-resize) is OK/live while P8 (persist width to storage) is DEAD. Whether 'resizable (P rows)' means reproduce only the live drag-resize or also revive width persistence is ambiguous and changes scope.
- [AMBIG] F7 amendment asks for both a 'selection status line' and an 'explicit clear affordance', and the Work line says '"what is selected" line + Esc/close affordance'. It is ambiguous whether Esc/close clears the selection (re-scoping back to none) or merely hides the inspector panel (legacy P1 behavior) — these are different behaviors with different DoD outcomes.
- [DODISS] 'row-walker green on P/Q/R' is not mechanically checkable for the owned DEAD rows P8 and R6: with no stated expected outcome (kept dead, implemented, or retired), 'green' is undefined for those rows.
- [DODISS] 'S4 acceptance: selecting a task re-scopes spans/events tracks (dimming) + inspector in one action' is checkable via T12 but not from T31's owned/dep surface — it requires the spans (T26) and events (T27) tracks, which are neither owned here nor listed as deps, so T31's PR alone cannot make this item pass.

## T32 impl=partial dod=no
- [MISSING] Cause of the 8993-vs-147 count discrepancy (S7): the linked S7 finding and inventory row F15 state the numbers but not why they differ; no doc explains it, so 'explain or fix in-diff' requires unguided code archaeology across viewer.html/flamegraph.js.
- [MISSING] How to implement K7 frame keyboard traversal (arrow-walk + Enter-zoom) without modifying the frozen flamegraph.js: its public API exposes no frame enumeration or focus-move hook, and T32 does not authorize editing the frozen core.
- [MISSING] Definition of which timeline time the new 'show in timeline' action navigates to for a frame that aggregates many samples spread across the selected region (single frame != single time point).
- [REFS] 'features/02 section S (S1-...: CPU/heap/idle/task-dump modes...)': section S's rows are labeled F1-F19, not S1-...; there are no S-numbered rows in section S. (The S-series lives in 04-ux-findings.md, a different document.)
- [REFS] Context calls the frame-keyboard-traversal a drop-in that 'lands here', but flamegraph.js (the frozen renderer that produces the frames) has no keyboard/frame-focus support and no API to add it externally.
- [AMBIG] 'S rows' / 'row-walker green on S': ambiguous whether this means rows in inventory section S (internally labeled F1-F19) vs the F-labeled rows of section F (Timeline header, owned by T25) — the F-prefix collision makes the owned row set unclear.
- [AMBIG] 'frame click offers show in timeline navigating the viewport': for an aggregated flamegraph frame spanning many samples, whether it navigates to the region start, the frame's first sample, or centers the whole span is unspecified and changes implementation.
- [AMBIG] 'count reconciliation documented' vs 'explained or fixed in-diff': unclear whether merely documenting why the two counts differ satisfies the DoD, or whether the on-screen numbers must be made consistent.
- [DODISS] 'count reconciliation documented' — existence of a doc is checkable but the correctness/adequacy of the reconciliation is a human judgment, not mechanically verifiable.
- [DODISS] 'row-walker green on S' — not mechanically checkable until the S-vs-F row-label ambiguity (which rows the walker checks) is resolved.
- [DODISS] 'frame click offers show in timeline navigating the viewport (new - ledger + inventory row added in-diff)' — checkable that navigation occurs, but 'correct' target viewport is undefined (see ambiguity), so pass/fail is not mechanical.

## T33 impl=partial dod=yes
- [MISSING] What the mock's 'red-flag detectors' are and whether they are in scope for the issues rail: concept-2.html says rail data = 'existing POI + red-flag detectors', but the ticket only names 'existing detectors' and the DoD only asserts POI count parity (74), so it is unclear whether an additional red-flag detector source must be built/wired here or elsewhere.
- [MISSING] Which POI filter/config the '74 candidates' count parity is asserted against (the default C2 filter option and worst-first state on the demo trace) is not stated, though it is inferable.
- [REFS] Context: 'POI data = existing detectors (features/02 E rows)' — the POI detectors live in section C (C2 filterPointsOfInterest / trace_analysis.js:875-970, C3 worst-first, C7 jump-to-POI), not section E. Section E (E1-E5) is time-mode/TZ/Set-Range/Clear-Range/URL-params and contains no detector logic.
- [AMBIG] 'Parse perf demoted to help/info menu (F2)' — unclear whether 'help/info menu' means the existing help overlay (T21-owned T rows), the D4/D5 uninstrumented-style info popup, or a new menu; the target surface changes where the D6/D7 parse-perf feature is relocated.
- [AMBIG] 'issues rail: sortable list (worker/kind/time/duration)' vs DoD 'same detector output as legacy' — legacy sorts POIs only by worst-first (severity) or chronological (C3); the four new sort keys have no legacy equivalent, so 'behavioral diff on detector numbers' could mean only the candidate set must match (not the ordering) — two readings affect what the differ checks.

## T34 impl=partial dod=no
- [MISSING] Issue #281's actual acceptance criteria: the DoD says 'Esc closes the load section (#281 acceptance)' but #281 is a GitHub issue whose content is not in the repo; without it you cannot verify you met 'the #281 acceptance'.
- [MISSING] The precise text/scope of the '04 finding D2' the ticket cites: no such finding exists in 04-ux-findings.md, so the 'load section not dismissible' requirement has no verifiable source spec (which surface, in which state, must become dismissible).
- [MISSING] The behavioral-differ and row-walker tooling contract (T12) that 'row-walker green on B' and 'behavioral-diffed' DoD items depend on is referenced but its usage/output format is not provided here (only named as chunk-1 T12).
- [REFS] '04 finding D2/#281' does not exist as a finding. In 04-ux-findings.md, 'D' is an evidence key (discoverability judge), not a finding ID; 'D1/D2' appear only inside the Evidence column of finding S3. The behavior the ticket describes ('New File destroys session without confirm') is finding S3, not a 'D2'.
- [REFS] S3 (the actual matching finding) does NOT mention 'load section not dismissible'; its text is: URL never changes, refresh/share loses analysis, New File discards without confirmation, no recovery. The 'load section not dismissible' claim is not supported by the cited finding.
- [REFS] The chunk-2 header's UX-amendment mapping (lines 12-18) maps feature sections to tickets but never assigns S3 to any ticket; T34 asserts ownership of the New-File/dismiss slice of S3 without S3 being listed in that mapping.
- [AMBIG] 'Esc closes the load section': ambiguous which surface. With no trace loaded the drop zone IS the whole page (nothing to reveal on dismiss); with a trace loaded there is a separate load/drop overlay (B5 #drag-overlay / replace view). Also B10 already binds Escape to cancelLoad during an active load. Which of these 'load section' means changes the implementation.
- [AMBIG] S3 is a broad task-blocking finding whose other parts (URL/deep-link state, recovery, copy-link) belong to T35/T38/S2-goto. T34 claims only the New-File-confirm + dismissible-load slice, but the boundary between T34's slice and those tickets for the 'no recovery / URL' parts of S3 is not explicitly drawn.
- [AMBIG] 'confirm before discarding a loaded trace' — no spec for the confirmation mechanism (native confirm() vs in-app modal/toast) or its exact trigger set (New File only, or also replace-by-drop B5 and Set/Clear Range reparse B14 which also mutate a loaded session).
- [DODISS] 'Esc closes the load section (#281 acceptance)': not mechanically checkable as written because (a) 'the load section' is ambiguous (see ambiguities) and (b) '#281 acceptance' points to external issue criteria not available in the provided context.
- [DODISS] 'New File now confirms when a trace is loaded': checkable in spirit, but 'confirms' has no defined pass condition (what the confirmation is, whether cancel must abort) — a reviewer cannot mechanically decide pass/fail without the mechanism spec.
- [DODISS] 'drop/pick/URL loads behavioral-diffed' depends on the T12 behavioral differ being defined for section B journeys; as written it names no specific journey/fixture, unlike sibling tickets that cite J-numbers, so 'behavioral-diffed' is not concretely checkable.

## T35 impl=partial dod=yes
- [MISSING] A multi-segment trace fixture whose tail is UNFETCHED (tier-1-only) for the primary DoD acceptance scenario: the embedded demo-trace.bin is the only referenced trace and nothing states it has multiple S3 segments or how to obtain/record such a fixture in a test harness.
- [MISSING] The POI-tick data source: Work says 'POI ticks from T33's detector data', but T33 is not in T35's Deps and (per chunk-2 ownership) T33 itself depends on T35 for markers - so the detector output shape/interface the minimap consumes is not available or specified as a dependency contract.
- [MISSING] The store viewport/selection slice interface (T07) that minimap drag and status-bar 'store bindings' write to/read from is required but T07 is not listed among T35's Deps (only reachable transitively via T21).
- [REFS] Context attributes 'segment listing extents (2.8 tier 1)' to T18, but T18 owns only the Parquet aggregate-endpoint client; per 02-architecture 2.8 the S3 listing metadata / per-segment extents come through T17's load.ts bootstrap, not T18's aggregates.ts.
- [REFS] Context lists T33 coordination for POI ticks only implicitly (via the Work line), while the Deps line omits T33 entirely despite the minimap POI ticks needing T33's detector data - the reference to that data source is not backed by a declared dependency.
- [AMBIG] 'POI ticks from T33's detector data' vs Deps not including T33: unclear whether POI ticks must ship in this ticket (blocked on T33) or are deferred/stubbed - two readings that change scope, since POI ticks are not mentioned in the DoD.
- [AMBIG] 'status bar reflects selection/range/progress' - 'progress segments' means the 2.8 per-segment fetch/parse feedback, but whether the status bar must drive real segment-window progress (requiring T17 wiring) or just render a progress model is unspecified.
- [AMBIG] 'copy-link button' in the status bar vs T19 already shipping 'a minimal toolbar button': unclear whether T35 replaces/relocates T19's button or adds a second copy-link surface.
- [DODISS] 'minimap navigates a multi-segment trace whose tail is UNFETCHED ... the 2.8 acceptance scenario' is checkable only if such a multi-segment fixture exists; the ticket names no fixture, so the check is not runnable as written without sourcing one.
- [DODISS] 'drag/click behavioral tests' does not state expected behavior/oracle (what drag/click must produce), so 'pass' is underspecified though nominally runnable.
- [DODISS] 'new rows walked by T12' depends on rows the ticket only says are 'added in-diff' with no enumerated list, so completeness of the walk cannot be verified against a fixed target.

## T36 impl=partial dod=no
- [MISSING] Interaction/visual spec for drag-reorder (grip location, drop behavior, animation) — section O covers only collapse, the gap table only says 'absent', and the concept-1 mock that shows 'track order drag-to-reorder' is not referenced by this ticket
- [MISSING] Definition of 'pin' for a track: what pinning does (pin-to-top? exempt-from-reorder? pin-open?) is not described in section O, the gap table, or any referenced doc
- [MISSING] Which uiPrefs fields to add for track order and pin state, and which ticket owns that slice schema — the uiPrefs slice (arch 2.4 line 179) today lists only 'foldable-panel states, sidebar width, legend toggles', and T36 lists no dep on the store ticket that owns it
- [AMBIG] 'pin' appears in the Goal title (collapse, reorder, pin) but is absent from Owns and from the DoD (collapse/reorder/persist) — unclear whether pin is actually in scope for this ticket
- [AMBIG] Vocabulary mismatch: goal/DoD say 'collapse' while the gap table says 'hide'; section O 'collapse' means a 24px minimized panel (not display:none removal) — unclear whether a collapsed track is minimized or fully hidden in the unified column
- [DODISS] 'collapse/reorder/persist walked' — Owns declares only 'features/02 O rows' (collapse+persist); reorder and pin have no owned inventory rows and, unlike T35, the ticket does not say to add new rows, so there is nothing for the T12 row-walker to 'walk' for the reorder/pin features
- [DODISS] 'O rows' amended semantics documented in-diff' relies on a ledger entry existing but gives no acceptance criterion for what the documentation must contain

## T37 impl=partial dod=no
- [MISSING] The authoritative finding-ID -> landing-ticket mapping is never consolidated anywhere. To produce the closure table T37 must reconstruct it by reading the Amendments lines of ~18 tickets (T15, T19-T36); no single reference lists which of the 23 findings (S1-S8, K1-K8, F1-F7) each ticket landed.
- [MISSING] For findings that no ticket owns, T37 must record 'OWNED+deferred (with reason)' or 'REJECTED (with sign-off)', but the reason/sign-off authority is external (maintainer) and not supplied. Concretely K1 (viewer search, task-blocking), K2 (browser page keyboard-dead, task-blocking, axe serious), and K3 (cross-page keyboard vocabulary) have no landing amendment in any chunk-2 ticket, yet T37 cannot invent their deferral reason or rejection sign-off.
- [MISSING] The DoD's 'all three new pages' is never enumerated in the ticket; presumably index/viewer/flamegraph (features/01/02/03), but the browser/browse page referenced by K2/J6/F6 ('three pages' in F6, 'browser page' findings) is not clearly one of the migrated pages, so whether its findings fall inside T37's axe/census scope is undetermined.
- [REFS] T37 Context asserts the 'Expected remainder' is only 'F2 leftovers on non-toolbar surfaces, K8 affordances outside lanes, empty-state copy, contrast stragglers (F6), and any finding a prior ticket descoped.' This understates the actual remainder: no chunk-2 ticket adds viewer search (K1, task-blocking) or fixes the browser page (K2 task-blocking / K3), so genuinely un-landed task-blocking findings remain, not just polish stragglers.
- [AMBIG] 'T12 axe/census clean' — the census (per T12) is an old-vs-new affordance differ. The whole point of the UX amendments is to deliberately change affordances, so a differ against legacy will never be empty. 'Census clean' has two readings: (a) zero diff vs legacy (impossible given intended changes), or (b) every diff is accounted-for/expected. These change what 'clean' means and whether the DoD is even achievable.
- [AMBIG] 'mapping every finding ID to its outcome + landing ticket' — for deferred/rejected findings there is no landing ticket; unclear what goes in that column (blank, N/A, or the ticket that decided to defer).
- [DODISS] 'a closure table ... mapping every finding ID to its outcome' — only the table's existence and completeness (all 23 IDs present) is mechanically checkable; the correctness of each outcome (whether a finding truly ended OWNED+landed vs should be deferred/rejected) is a judgment call, not mechanically verifiable.
- [DODISS] The Goal's 'verify none [of the earlier findings] regressed' has no corresponding checkable DoD item; the DoD only requires the table plus axe/census, so the regression-verification obligation is unmeasured.
- [DODISS] 'T12 census clean across all three new pages' is not a checkable pass/fail as written because the census is a legacy-vs-new differ that will report intended affordance changes; without a defined acceptance threshold ('clean' = what count) it cannot be mechanically judged.

## T38 impl=partial dod=no
- [MISSING] Mechanism for injecting the switch control into legacy pages: legacy pages are served as byte-copied static HTML via T02's static-copy list (no build/module step), so how a 'minimal injected element' is added to them without editing the frozen/legacy files is not specified anywhere the ticket links.
- [MISSING] How the localStorage preference concretely selects which version is served/rendered ('biases the default') given the routing convention is left undecided - the interaction between the persisted pref, the query-string, and the default-until-flip rule is not pinned down.
- [REFS] ADR 'section 8 stages 1-2 / three-stage plan': section 8 is loose prose ('a-step one... Then... Finally') and never numbers discrete stages, so the ticket's 'stages 1-2' / 'stage 3' (T39) labeling is an interpretation, not a verbatim reference. It is close enough to not be wrong, but the crisp stage numbering the ticket implies does not exist in the ADR.
- [AMBIG] 'T12 census asserts the control's presence': T12's census (chunk-1) is defined as an old-vs-new affordance dump + differ. Asserting a control is present on BOTH the legacy and new page is a presence check, not a diff (and other tickets treat the switch as a census DIFF entry: '+switch control, ledger'). Whether the census self-asserts presence or just flags it as an expected diff changes what T38 must wire up.
- [AMBIG] Routing convention ('?ui=new' vs 'new/<page>.html') is explicitly delegated to the ticket, but the two options have materially different consequences for query-string preservation and for T02's static-copy list (a parallel path implies duplicated served files); which one is chosen changes the implementation substantially and is left open.
- [DODISS] 'every page with a migrated version shows the switch on BOTH versions': T38 depends only on T02 and BLOCKS T13/T14/T41/T21, so at T38's completion NO migrated pages exist. This item cannot be checked when T38 lands - it is only verifiable once the consuming tickets land, which is outside T38's scope.
- [DODISS] 'round-trip switch keeps the same trace loaded on all pages' - same problem: no migrated pages exist yet to round-trip against, so not mechanically checkable at T38 completion.
- [DODISS] 'no view state leaks across (test: zoom in new viewer, switch, legacy opens default view)' - requires the new viewer (T21), which depends on T38. Not executable within T38.
- [DODISS] 'N10 deep links work in both' - 'both' (both UIs) requires a migrated counterpart page that does not exist at T38 time; only checkable in combination with a consumer ticket.

## T39 impl=partial dod=no
- [MISSING] Where the migration 'ledger' lives and its format: the DoD requires 'every ledger entry has maintainer sign-off recorded' but neither the ADR nor the inventory docs define or locate any ledger (the word 'ledger' appears in no referenced doc, only in the tickets).
- [MISSING] Source of the 'recorded fixtures' of the last legacy build needed for the behavioral differ: T39 deletes the legacy pages, so the old-vs-new differ (T12) can no longer run live; no ticket/doc reference explains where or when these fixtures are recorded.
- [MISSING] Identity/source of 'a real large trace' required for the NFR budget runs; no large-trace fixture or generator is referenced (demo trace and x8 stress exist, but the real large trace does not).
- [MISSING] What counts as a recorded 'maintainer sign-off' and where it is stored (file, PR approval, ADR note).
- [REFS] T39 Context attributes the 'layer plan (final gate = full-inventory walk + census + behavioral diff + NFR budgets on demo AND a large trace)' to the 'chunk-1 header', but the chunk-1 header (chunk-1-foundation.md lines 1-27) contains only conventions and an ownership summary; no layer plan or final-gate definition. The four verification layers actually live in ADR section 7 and T12's context. (T12 makes the same misattribution.)
- [REFS] T39 Work says 'NFR budget runs (N1-N19)', but most of N1-N19 (defined in 02-architecture.md section 1) are process/discipline rules, not runnable budgets: N7 dependency discipline, N8 zero backend change, N9 format compat, N10 URL stability, N12 install paths, N13 test runner, N14 dev loop, N15 migration safety, N16 strict typing, N17 chrome sync, N18 dev assertions. Only a subset (N1, N2, N3, N4, N19 plus the heap budget) are measurable runtime budgets, so 'budget runs (N1-N19)' overstates what is executable.
- [AMBIG] 'MAX_OPEN_BYTES guard replaced by the resident-window budget': MAX_OPEN_BYTES is defined in heatmap.js:27 (features/01 H4), which is frozen core per the chunk-1 header. Unclear whether lifting the cap / amending H4 requires editing frozen core (forbidden) or only touching the browse-page warning and viewer wrapper.
- [AMBIG] 'ADR section 8 stage 3': the ADR section 8 describes three stages prosaically but never numbers them, so which prose sentence is 'stage 3' (remove legacy) must be inferred.
- [DODISS] 'functional contract holds (full row-walk report committed)': not mechanically checkable without a defined pass threshold. The row-walker emits VERIFIED/FAILED/NOT-TRIGGERABLE (features/01 records many NOT-TRIGGERABLE rows due to demo-data limits); the ticket does not say whether NOT-TRIGGERABLE rows pass or block the gate.
- [DODISS] 'NFR budgets hold': not mechanically checkable for the many non-numeric NFRs in N1-N19, and no numeric target is given for the 'real large trace' run.
- [DODISS] 'every ledger entry has maintainer sign-off recorded': not checkable because no ledger location/format is defined in any referenced doc.
- [DODISS] Behavioral differ 'vs the last legacy build (recorded fixtures)': not executable after legacy deletion unless the fixtures already exist; no reference defines where they come from or who records them.

## T40 impl=yes dod=no
- [MISSING] How to get the tokio-stats page to serve walkable data under the dev-server: dev_server.rs constructs AppState with agg=None (server/mod.rs:115) and only opens the CONDITIONAL gate via .with_byo_creds(true) (dev_server.rs:95). It is unstated whether /api/tokio-stats returns real, row-walkable data in that configuration or how to populate an AggContext, which the DoD's 'live-walked against the dev-server' step needs.
- [MISSING] The features/01 'Tokio Stats' entry-point rows the ticket points to do not yet exist, and the ticket does not say whether T40 should wait for / cross-link them or document the entry point itself from index.html:357.
- [REFS] Context claims the entry point is 'browser-page \'Tokio Stats\' button (features/01 rows added by T01)'. features/01-index-html.md contains NO Tokio Stats button rows (grep finds none). The button exists at dial9-viewer/ui/index.html:357-358 (viewTokioStats -> opens tokio_stats.html at :1785), but T01 has not added inventory rows for it, and T01 is not listed among T40's Deps (only T12).
- [REFS] Source paths omit the crate prefix: actual files are dial9-viewer/src/server/tokio_stats.rs and dial9-viewer/src/server/config.rs, not src/server/*. 'config.rs' is ambiguous (three config.rs exist in the repo); the aggregation_enabled gate is specifically dial9-viewer/src/server/config.rs:14,30.
- [AMBIG] 'run the page against the dev-server (aggregation on)': the dev-server turns aggregation_enabled true only via with_byo_creds(true) (config.rs:30), not a real AggContext (agg=None). Unclear whether 'aggregation on' means merely opening the UI gate or exercising actual aggregate data, which changes what a full live-walk must cover.
- [AMBIG] Validation mechanism conflict: Deps says T12 '(row-walker used for validation)', but Context says the validation method is features/01's Playwright 'Live validation results' live-walk. These are two different tools; unclear which T40's DoD requires.
- [DODISS] 'every control/interaction/URL-param/conditional state of the page has a row with verified access path + file:line source' has no defined completeness oracle — verifying 'every' is subjective enumeration against the HTML, not a mechanical pass/fail check.
- [DODISS] 'live-walked against the dev-server' is not a checkable artifact: unlike features/01 (which requires a 'Live validation results' section + results.json/summary.md), T40 does not require producing any validation record, so 'was it live-walked' cannot be verified from the deliverable.
- [DODISS] 'ownership summaries in this file + chunk-1 updated' — 'this file' is ambiguous between the new features/04 doc and chunk-3-post.md; and the chunk-1 ownership summary currently has no features/04 line, so the expected end-state to check against is not spelled out.

## T41 impl=partial dod=no
- [MISSING] features/04 (docs/ui-inventory/features/04-tokio-stats-html.md) does not exist yet - only features/01..03 are present. It is produced by the dependency T40, so the row set that defines 'ALL rows' and the DoD's row-walker target is undefined until T40 lands.
- [MISSING] No new-UI scaffold exists in the repo: there is no src/pages/, no Vite config, and no T13/T14-migrated page to copy. The 'same treatment as T13/T14' exemplar and the 'new-UI path (T38)' are entirely unrealized, so the concrete component/build pattern to follow is not available from provided context.
- [MISSING] T18's typed aggregates client (the thing T41 must 'reuse, do not duplicate') does not exist yet, so there is no artifact to consume; only the legacy raw fetch to /api/tokio-stats in tokio_stats.html exists.
- [MISSING] The exact set of 'scope params' the switch round-trip must preserve is unspecified; the legacy page carries bucket, prefix, service, host (multi), start_ns, end_ns, refine - which of these count as 'scope' is not stated.
- [REFS] src/server/tokio_stats.rs and src/server/config.rs referenced in the T40 context (which T41 relies on) are actually at dial9-viewer/src/server/... - the ticket paths omit the dial9-viewer/ crate prefix. They resolve, but the bare paths are not correct as written from repo root.
- [AMBIG] 'unified keyboard model (T20) integrated' - the legacy tokio_stats.html has no keyboard shortcuts (only slider input / change / click handlers), so it is unclear what keyboard behavior is to be integrated or verified for this page.
- [AMBIG] 'switch round-trip keeps scope params' has two readings: preserve only the S3/scope selectors (bucket/prefix/service/host) or all URL params including start_ns/end_ns/refine time-window state.
- [AMBIG] N17 is cited as 'the declarative-template rule' and 'the structural guard' for XSS; N17 (02-architecture.md:81) is actually 'Chrome cannot desynchronize from state' (declarative render from store), so whether the escaping guarantee follows from N17 specifically vs section 2.4/R2 templating is open to interpretation.
- [DODISS] 'T12 full pass on features/04' is not mechanically checkable as written because features/04 does not yet exist (blocked on T40); the row/census baseline it diffs against is undefined.
- [DODISS] 'switch round-trip keeps scope params' is not checkable without a defined enumerated set of 'scope params' (see ambiguity).
- [DODISS] 'census diff vs legacy page' and 'behavioral differ on the stats numbers - exact match' depend on T12 tooling (row-walker/differ) that must exist and be pointed at this page; the DoD assumes but does not confirm that tooling supports the tokio-stats page.

## T42 impl=partial dod=no
- [MISSING] The producer-side trace/S3 fixture format: how boot_id transitions, coverage gaps, seams, segment boundaries, and multiple hosts are actually encoded in the trace bytes and the S3 key layout. The ticket points to encoder.rs (a low-level generic event writer) and to features/01 which only describes the CONSUMER side (heatmap.js line refs for gaps/seams/boot dividers); neither gives the schema/field semantics a generator must emit.
- [MISSING] The S3 object-key layout convention for multi-host/multi-segment sets (dev_server seeds one hardcoded key like traces/2026-04-09/1900/demo-service/local/host-0/abcd/...); the ticket does not specify or link the layout rules the fixtures must follow to trigger multi-row/boot/gap heatmap cases.
- [MISSING] What issue #596 ('group-by-runtime') defines as a multi-runtime event — the issue is not readable from the repo, only referenced.
- [MISSING] T12's interface for 'the row-walker can request the fixture a row needs' — the mechanism by which a row selects/requests a fixture is not specified here (owned by dep T12, but the wiring contract is undefined in this ticket).
- [REFS] Context lists the dev-server seeding as `src/bin/dev_server.rs`; the actual path is `dial9-viewer/src/bin/dev_server.rs` (crate prefix omitted, unlike the fully-qualified `dial9-trace-format/src/encoder.rs` beside it).
- [REFS] Context calls `scripts/regenerate_demo_trace.sh` the 'metrics-service demo generator' with a `--demo` flag; the script itself invokes the metrics-service binary with `--demo` and only produces a single-host/single-segment real-time trace (confirmed by features/01's coverage caveat), so it is not a source of the multi-host/boot/segment fixtures this ticket needs — the reference overstates its capability.
- [AMBIG] 'a large (>=100 MB raw) multi-segment set' + DoD 'T39 large-trace budget run has a reproducible input': unclear whether a 100 MB fixture is checked into the repo or generated on demand by the script/bin at run time — the two readings differ materially (repo bloat vs. generation-time cost).
- [AMBIG] 'wire into T12 so the row-walker can request the fixture a row needs': ambiguous whether T42 defines a new fixture-request API consumed by T12, or merely drops fixtures into a location T12 already knows how to read.
- [DODISS] 'previously NOT-TRIGGERABLE rows from features/01 validation now walk green against the fixtures (list them in the PR)' — the goal/context enumerate an open-ended set (F5/F7/F8/F9, H4, C7, D4, plus '...'), so 'previously NOT-TRIGGERABLE rows' has no fixed checklist; without an exact target row list the 'now walk green' pass is not mechanically bounded (e.g. C7 is blocked by C6 filter, H5 is tied to H4 — unclear if those are in scope).
- [DODISS] 'T17s simulated scenario upgraded from repeated-demo to real fixtures' — checkability requires knowing what T17's 'simulated scenario' and 'repeated-demo' concretely are; not defined in this ticket, only resolvable by reading dep T17.
- [DODISS] 'T39s large trace budget run has a reproducible input' — 'reproducible input' is not given an acceptance criterion (fixed byte size? deterministic bytes? committed artifact vs. seeded RNG?), so it cannot be checked as written.

## T43 impl=partial dod=no
- [MISSING] Actual content/acceptance criteria of GitHub issue #303 - issue-tracker.md says issues live only in GitHub (via `gh`), so the ticket's one-line paraphrase ('agents cannot set time range, highlight, open flamegraph') is the only spec available; what exactly makes #303 'closeable' is not in the repo.
- [MISSING] Which URL representation the skills should emit for 'window + selection': T19's view-state lives in a versioned URL hash codec, while trace source and the N10 window params (`from`/`to`) are query params. The ticket lists these as one schema but does not say whether selection/viewport go in the hash (T19) or as query params for the doc/skills to emit.
- [MISSING] The current dial9-html-report deep-links use `start=`/`end=` query params (SKILL.md:94), which are NOT in N10's documented param list (`trace,svc,host,from,to,segs,bucket,prof`). The ticket does not say whether to reconcile these to `from`/`to` or keep `start`/`end`.
- [REFS] Work item says document the schema in `ui/README.md`; no such path exists - the actual file is `dial9-viewer/ui/README.md` (resolvable by convention but the literal path is wrong).
- [AMBIG] 'document the full URL schema (trace source, viewport, selection, POI, time mode)' - unclear whether this is one unified schema or the union of query params (trace source, N10) + hash codec (T19 view-state); the two live in different parts of the URL and skills would emit them differently.
- [AMBIG] 'open the viewer at an exact window with a selection' - 'selection' could mean a task/row highlight or a time-range selection; T19 lists both viewport and selection separately, so which the DoD test must assert is ambiguous.
- [DODISS] '#303 closeable with a link to the doc' is not mechanically checkable from repo context - #303's acceptance criteria live in GitHub, so 'closeable' is a judgment call, not a pass/fail check.
- [DODISS] 'an agent (or curl-constructed URL) can open the viewer at an exact window with a selection, verified by a T12 script' presumes a T12 script exists that can assert the viewer restored the exact window+selection; the check is only mechanical once that script is written, and the pass criteria (pixel window? state values?) are unspecified.

## T44 impl=partial dod=no
- [MISSING] The actual content of the GitHub issues being closed (#55, #191, #281, #282, #137, #421/#523, #480, #593, #68, #248, #303): only bare numbers are given, so drafting accurate close-comments with matching titles/wording requires the live issue tracker, which is not in the repo.
- [MISSING] Where 'evidence links' should point: the ticket does not say whether evidence = merged PR/commit SHAs, ticket doc anchors, or screenshots, and most referenced tickets (T17/T19/T23/T25/T29/T34/T45/T48) must have LANDED to have evidence but their landed status/artifacts are not resolvable from this ticket alone.
- [MISSING] The location/format for 'maintainer sign-off recorded' - no file, field, or convention is specified for recording sign-off.
- [MISSING] Where the 'draft close-comments' should live (which file/path under docs/tickets/ or elsewhere).
- [REFS] Context says the friction-mining catalog is 'in docs/ui-inventory/04-ux-findings.md method notes + the issue list', implying that file is the source of the issue->ticket mapping. It is not: the Method section (lines 8-32) contains no issue list, and most cited issues (#55, #191, #421, #523, #480, #552, #593, #68, #248, #303) appear nowhere in 04-ux-findings.md. Only #137, #281, #282 (plus #450, #571 which the ticket does not use) appear there. #55 appears only in finding F5 (line 89).
- [REFS] The 'F5 finding' is cited as the driver ('shipped capabilities must leave a trace'). F5 does exist (line 89) and matches that theme, but it is scoped to gesture/hint-chip discoverability and issue #55 only - it does not enumerate or support the broader issue->ticket map the ticket attaches to it.
- [AMBIG] 'final sweep after T39': T39 is not in this chunk (chunk 3 covers T40-T48) and is not defined here; which ticket/chunk owns T39 and what 'sweep' entails is unclear.
- [AMBIG] Whether the DoD is considered complete when the mapping table + draft comments exist but issues are NOT yet closed (closing is gated on maintainer sign-off and done by a human). The ticket both 'Owns: none (process)' and lists 'issues actually closed' in the DoD, giving two readings of what 'done' means for the agent.
- [DODISS] 'issues actually closed only with maintainer sign-off recorded' is not mechanically checkable by the implementing agent: it depends on an external maintainer action and on a 'recording' whose location/format is undefined.
- [DODISS] 'draft close-comments prepared' is not mechanically verifiable without a defined artifact location or acceptance criterion for what counts as a complete draft per issue.
- [DODISS] The mapping table item is only partly checkable: existence of a table in docs/tickets/ is verifiable, but 'evidence' completeness/correctness cannot be checked without a defined evidence format and resolvable landed-ticket artifacts.

## T45 impl=partial dod=no
- [MISSING] Which segmentMetadata Map keys hold service and host: the parse side stores arbitrary user-supplied key/value pairs and only 'service'/'host' in a test convention (writer.rs:1536); no documented contract pins the key names the viewer must read.
- [MISSING] A trace/fixture for the file-drop validation path that actually contains service/host in SegmentMetadata: the default writer adds only version + process.available_parallelism, so it is unclear whether the demo trace (used to 'walk the row') carries service/host at all.
- [REFS] Ticket says svc/host are passed 'via traceTitleParams' as trace identity, but traceTitleParams (index.html:1686) derives svc/host from parseKey(objectKey), not from the trace's SegmentMetadata — the two sources are different in origin.
- [REFS] Referenced features/01 I3 cites source line 1457-1473 for traceTitleParams, but that range is setupHeatmapInteraction; traceTitleParams is actually at index.html:1683-1702.
- [AMBIG] 'metadata wins when both present' is unspecified for multi-segment/multi-host file-drop loads: the parser keeps only the latest segment's metadata (trace_parser.js:666), so which service/host to display when segments differ is undefined.
- [AMBIG] 'service/host from trace metadata' does not state the exact key names to look up in segmentMetadata (e.g. 'service' vs 'svc' vs 'process.service'), which directly changes the implementation.
- [DODISS] '#68 closeable via T44' is not mechanically checkable within this ticket — it depends on T44's process/maintainer-sign-off flow, not on any artifact produced here.
- [DODISS] 'viewer shows service/host from trace metadata for file-drop loads' is only checkable if a trace file carrying service/host SegmentMetadata is available; no such fixture is identified, so the check cannot be executed as written.

## T46 impl=partial dod=no
- [MISSING] The final Vitest test-running commands / conventions needed to rewrite the AGENTS.md testing section come from T10/T11, not from T46 or its links; without those tickets merged the ticket cannot state what 'reality' is.
- [MISSING] The final rollout-switch URL convention (T38 explicitly leaves undecided: `?ui=new` vs `new/<page>.html`) is needed to 'document the rollout switch for users' but is not fixed anywhere the ticket links to.
- [MISSING] Relationship/authority between the two near-identical viewer READMEs (top-level README.md is a symlink to dial9-tokio-telemetry/README.md; dial9-viewer/README_TELEMETRY.md is a separate copy with identical content) is unspecified — unclear whether one, the other, or both must be edited and kept in sync.
- [REFS] DoD requires removing 'serve.py references', but no serve.py reference exists in any document T46 rewrites (dial9-viewer/ui/README.md, dial9-viewer/README_TELEMETRY.md, top-level README, AGENTS.md). The only serve.py mentions live in docs/tickets/*.md and docs/ui-inventory/features/01-index-html.md, which T46 does not own. The README's 'dial9 serve' CLI references are legitimate and unrelated.
- [REFS] Context lists 'README_TELEMETRY.md' as if top-level; the file is actually at dial9-viewer/README_TELEMETRY.md. The genuine top-level README is a symlink to dial9-tokio-telemetry/README.md.
- [REFS] Goal calls it a 'staged-rollout switch' while the referenced T38 is titled 'Dual-UI raw switch' with a maintainer decision of NO view-state porting ('raw switch'); the naming differs though the underlying mechanism matches.
- [AMBIG] 'AGENTS.md testing rules match reality (Vitest, no hand registration)' assumes T10/T11 have already replaced the node/e2e-trace-tests.sh workflow with Vitest. If those deps are not yet merged, current 'reality' is still node scripts + hand registration, so T46 cannot truthfully assert Vitest — the ticket is entirely gated on external state and 'reality' is a moving target.
- [AMBIG] 'docs link to it rather than duplicating' (help-overlay as source of truth) does not specify a stable anchor/URL to link to, so 'link to it' has two readings: link to the in-app overlay UI vs. link to T20's keyboard.html spec.
- [DODISS] 'ui README covers dev/test/parity/URL-contract' — 'covers' is subjective with no acceptance criterion; not mechanically checkable.
- [DODISS] 'rollout switch documented for users' — subjective; no defined minimum content or checkpoint.
- [DODISS] 'AGENTS.md testing rules match reality (Vitest, no hand registration)' — checkability depends on whether T10/T11 have landed; against current repo state (node + e2e-trace-tests.sh registration) it cannot be satisfied, so the check is not self-contained.
- [DODISS] 'stale instructions removed (serve.py references ...)' — not satisfiable/verifiable against the target docs because they contain no serve.py references to remove; only the e2e-trace-tests registration-warning part is grep-checkable.

## T47 impl=yes dod=no
- [MISSING] The 'session notes' that supposedly hold hypotheses H1-H8 of the data-model sweep are not in the repo. Only F4 of 03-performance-findings.md is available and it summarizes ~5 mechanisms, not 8 labeled hypotheses. If the ADR must cite H1-H8 individually, that source is absent.
- [MISSING] T39 (the trigger dependency: 'viewer parity gates passed') is defined in another chunk file, not chunk-3-post.md; its exact gate criteria are not resolvable from this ticket's context alone.
- [REFS] Ticket claims 'hypotheses H1-H8 of the data-model sweep are in the session notes summarized there' (F4). F4 enumerates 5 mechanisms (polymorphic events[], triple poll materialization, dead fields, decoder churn, un-interned callchains) with no H1-H8 labels and no 8-item list. The 'H1-H8' framing does not match what F4 says.
- [REFS] Two files are numbered ADR-0004 (docs/adr/0003-folded-set-is-the-output-listing.md and 0004-viewer-ui-migration.md). 'ADR-0004 section 6' only resolves against the viewer-ui-migration file; the bare '0003' reference is ambiguous given the collision.
- [REFS] trace_analysis.js:39 is cited for the 'getTraceTimeRange N-array' micro-fix; line 39 is the processResourceUsageTimestamps .map, while the per-event N-array materialization (events.map(e=>e.timestamp)) is line 40. Function is correct, line is off by one. decode.js:50 (one-TextDecoder reuse) verified accurate.
- [AMBIG] The new ADR is to be numbered 'ADR-0004', but the existing duplicate use of '0003' means the numbering scheme is unclear (whether 0004 is actually the next free number).
- [AMBIG] 'the batched micro-fixes' plus 'ticket partition' — unclear whether the micro-fix batch (decode.js/trace_analysis.js one-liners) becomes its own partition entry or is folded into the reshape implementation tickets. ADR-0004 section 6 says micro-fixes are 'batched into the reshape ADR', supporting inclusion, but the split is not specified.
- [DODISS] 'maintainer review gate - implementation tickets only after acceptance' is a human/process gate external to the deliverable; it cannot be mechanically checked at the time the ticket (authoring ADR-0004) is completed.
- [DODISS] 'a ticket partition' is checkable for existence but its completeness/correctness (that it correctly decomposes the reshape) is a subjective judgment, not mechanically verifiable.

## T48 impl=partial dod=no
- [MISSING] The exact reproduction and expected-vs-actual behavior from GitHub issue #593 — only a one-line restatement ('reported match percentage does not match visibly highlighted samples') is in-repo; the issue body lives on GitHub (docs/agents/issue-tracker.md says issues are GitHub-only, requires gh), so the precise defect spec cannot be confirmed from provided context.
- [MISSING] What surface counts as 'page glue (fixable freely)': the entire search-count computation (countSearchMatches, updateSearchStats, the percentage) lives in flamegraph.js, which architecture.md line 140-141 lists as frozen core. It is unclear what a page-side fix location would even be, since the defective logic is in the frozen file.
- [AMBIG] 'if page-side (fixable freely)' vs 'core-side -> T47': because the percentage logic is entirely inside frozen flamegraph.js (countSearchMatches at :106-118, updateSearchStats at :485-518), whether any page-side fix is possible at all is ambiguous, and this determines whether the work lands here or is deferred to T47.
- [AMBIG] The defect itself has two reasonable 'correct' readings: F38's percentage is matchedSelf/totalSelf (self-samples only), while F40 dims at full frame level (matches include descendant width). The ticket does not state whether the fix should make the percentage reflect highlighted frame area or change the highlighting — different implementations.
- [DODISS] 'root cause documented' names no artifact or location, so there is no mechanical check for where/whether it is done.
- [DODISS] The DoD is branchy ('if page-side: fixed + regression test + amended rows; if core-side: deferred to T47') — which branch applies is unresolved until investigation, so the completion criteria are not fixed/checkable up front.
- [DODISS] '#593 gets a status comment via T44' requires an external GitHub action and is gated by T44's maintainer-sign-off process; it cannot be verified from the repo.
